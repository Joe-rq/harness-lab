# REQ-2026-076 QA 报告

> REQ: [REQ-2026-076](../completed/REQ-2026-076-stage-3-worktree-namespace-isolation-and-stage-2-revalidation.md)
> QA 日期: 2026-06-04

## 验证证据

### 1. `node tests/event-store.test.mjs`

结果:

```text
All event-store tests passed (20).
```

覆盖证据:
- `testSameSessionWritesToSeparateWorktreeNamespaces`:同一 `sessionId` 在 `feature/a` 与 `feature/b` 下写到不同 jsonl 文件
- `testReadEventsIncludesLegacyMainNamespaceAndArchiveFiles`:旧 main 路径、namespace 路径、archive 路径全部可读
- `testStage2RevalidationUsesDefaultNamespacedWrites`:S2-CP5/S2-CP6 重验 fixture 覆盖独立 projection 与 duplicate conflict 报告

### 2. `npm run req:status -- --all`

结果:

```text
Worktree statuses (1):
  main: REQ-2026-076 (design)
```

说明:该命令在 implementation 重新 start 前执行,显示旧事件 projection 可读;随后 `req:start --phase implementation` 已写入 `.claude/worktrees/main/events/session-main.jsonl`,证明新 namespace 写入路径生效。

### 3. `node scripts/event-store.mjs stats --metrics`

结果摘录:

```text
Total events: 17
req_started: 5
req_completed: 5
failure_rate: 0.000 ratio (enabled=true, threshold=0.3)
parallel_req_count: 5.000 count (enabled=true, threshold=3)
```

说明:旧 `.claude/events/session-main.jsonl` 的 0.9 legacy 事件仍可兼容读取;REQ-075 metrics 没有因 REQ-076 路径变更失效。

### 4. `rg 'payload\.observation|observation":true|observation=true' .claude requirements docs`

结果:命中文档说明和 progress 待办,未命中 `.claude/events` 或 `.claude/worktrees` 下的真实事件。

处理结论:
- 当前事件账本没有 `payload.observation=true` 过渡事件,无需迁移。
- progress 已改为正式 `s3_observation_window_start` 写入待办,不再要求使用临时 `payload.observation=true`。
- REQ-075 的历史文档引用作为债务来源保留,不阻塞 REQ-076 收口。

### 5. 人工验证:QA 证据核对

人工核对内容:

- `tests/event-store.test.mjs` 的 S2 重验 fixture 覆盖 `feature/a`、`feature/b`、`feature/c` 三个 worktree namespace。
- fixture 断言 `feature/a` 与 `feature/b` 使用同一个 `shared-session` 但写入不同文件。
- fixture 断言重复 active `REQ-TEST-A` 只出现在 `aggregation.conflicts`,没有自动合并或改写事件文件。
- 真实工作区已出现 `.claude/worktrees/main/events/session-main.jsonl`,其中包含 REQ-2026-076 implementation 事件,证明新默认写入路径已生效。

人工验证结论:PASS。

## 验收标准逐条核对

| AC | 状态 | 证据 |
|----|------|------|
| AC-1 worktree namespace 写入 | ✅ | `testSameSessionWritesToSeparateWorktreeNamespaces`;真实 `.claude/worktrees/main/events/session-main.jsonl` |
| AC-2 旧 main 路径可读 | ✅ | `testReadEventsIncludesLegacyMainNamespaceAndArchiveFiles`;`stats --metrics` legacy warn 后成功输出 |
| AC-3 namespace + archive 可读 | ✅ | `testReadEventsIncludesLegacyMainNamespaceAndArchiveFiles` |
| AC-4 多 worktree projection | ✅ | `testStage2RevalidationUsesDefaultNamespacedWrites` |
| AC-5 conflict 只报告 | ✅ | `testStage2RevalidationUsesDefaultNamespacedWrites` 精确断言 duplicate conflict |
| AC-6 S2 重验落盘 | ✅ | 本 QA §1 记录 S2-CP5/S2-CP6 fixture |
| AC-7 observation 临时债务 | ✅ | 本 QA §4 |
| AC-8 全量验证 | ✅ | `node tests/event-store.test.mjs`、`npm test`、`npm run docs:verify`、`npm run check:governance` 全部通过 |

## 元反思检查

- ✅ 目标实现:namespace 写入路径和 S2 重验 fixture 已落地。
- ✅ 旧功能保护:旧事件路径、archive、REQ-075 metrics 仍可读。
- ✅ 逻辑正确性:冲突只报告,没有自动合并或改写事件。
- ✅ 完整性:main/worktree/archive/坏事件边界均有测试。
- ✅ 可维护性:路径计算集中,调用方不需要自行拼目录。

## 结论

REQ-2026-076 通过 QA,已完成收口。
