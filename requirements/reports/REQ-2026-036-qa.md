# REQ-2026-036 QA 报告

> 2026-04-18

## 测试执行

### 自动化测试
```
npm test → All governance tests passed (11/11)
```

### 手动测试

| 测试场景 | 命令 | 预期 | 实际 | 结果 |
|---------|------|------|------|------|
| --scan 扫描 | `node scripts/invariant-extractor.mjs --scan` | 提取候选 | 21 条候选 | ✅ |
| --check requirements/ | `--check --file requirements/in-progress/test.md` | 触发 INV-002, INV-003 | 如预期 | ✅ |
| --check README.md | `--check --file README.md` | 触发 INV-001 | 如预期 | ✅ |
| --check scripts/ | `--check --file scripts/req-check.sh` | 无触发 | 如预期 | ✅ |
| --check experience/ | `--check --file context/experience/test.md` | 触发 INV-002 | 如预期 | ✅ |
| req-check.sh 集成 | `echo '...' | bash req-check.sh` | 提醒 + exit 0 | 如预期 | ✅ |
| 执行时间 | `time node invariant-extractor.mjs --check ...` | < 2s | 0.46s | ✅ |

## 质量评估

- 不变量匹配精度：3/3 种子规则在预期路径触发，无误报
- 性能：0.46s，不增加明显延迟
- 向后兼容：所有 11 个现有测试通过

## 已知限制

1. --scan 自动提取质量较低（21 条中多数为噪声），需要人工精选
2. glob 匹配使用简化正则，对复杂模式（如 `!` 排除）不支持
3. 不变量规则的 message 块使用 YAML 多行字符串 `|`，frontmatter 解析较脆弱

## 结论

QA 通过，可以完成 REQ。
