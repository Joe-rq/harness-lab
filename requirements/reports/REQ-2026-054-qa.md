# QA: REQ-2026-054

## 状态
- ✅ 通过

## 验收标准检查

| # | 验收标准 | 结果 | 证据 |
|---|---------|------|------|
| 1 | `req:status --json` 输出合法 JSON，包含 active REQ 完整状态 | ✅ | `testStatusJsonWithActiveReq` 通过 |
| 2 | 无活跃 REQ 时返回 `{"active_req": null}` | ✅ | `testStatusJsonNoActiveReq` 通过 |
| 3 | `external-mappings.json` 不存在时 external 为 null；存在时返回映射 | ✅ | `testStatusJsonWithExternalMapping` 通过 |
| 4 | 格式错误时返回 warning 而非崩溃 | ✅ | `testStatusJsonWithCorruptedMapping` + `testStatusJsonWithInvalidMappingStructure` 通过 |
| 5 | `req:status`（不带 --json）保持人类可读文本 | ✅ | `testStatusTextMode` 通过 |
| 6 | 不变量 INV-058 已创建且内容完整 | ✅ | 文件存在于 `context/invariants/`，包含 5 条规则 + Why + How to apply |
| 7 | `npm test` 全部通过 | ✅ | 11/11 通过 |
| 8 | `npm run check:governance` 全部通过 | ✅ | "Governance check passed." |

## 自动化测试结果

```
tests/governance.test.mjs: 11/11 PASS
tests/req-status-json.test.mjs: 7/7 PASS
```

## 手动验证

- `node scripts/req-cli.mjs status --json`：输出合法 JSON，字段完整
- `node scripts/req-cli.mjs status`：输出人类可读文本
- 损坏映射文件测试：返回 warning，不崩溃
- 重复映射测试：检测到 duplicate req_id 并 warning

## 元反思检查

- **目标实现**：3 个目标全部达成。`req:status --json`、external-mappings.json 读取、INV-058 均已实现。
- **旧功能保护**：现有命令（create/start/block/complete）行为未变，11 个既有测试全部通过。
- **逻辑正确性**：覆盖了无活跃 REQ、有活跃 REQ、映射文件存在/缺失/损坏/结构错误/重复映射 6 种场景。
- **完整性**：`readiness` 字段为编排器提供了可执行判断依据，`missing_reports` 提供了缺失信息。`updated_at` 为 null 是已知限制。
- **可维护性**：statusCommand 遵循 req-cli.mjs 现有模式（readReq、parseBulletLines 等复用），无新依赖。

## 对齐检查

- **目标对齐**：实现服务于「让编排器能消费 REQ 状态」，没有偏离。
- **设计对齐**：遵守了「CLI owns schema」原则，映射文件由 CLI 管理，编排器只消费。
- **验收标准对齐**：8 项验收标准全部满足。

## 已知限制（后续迭代）

1. `updated_at` 始终为 null——可通过 git log 补充
2. `verification_criteria` 保留 `- [ ]` 前缀——编排器需自行清理
3. `req:import` 未实现——留给 Phase 6b
4. 单个活跃 REQ 限制——当前架构只支持一个活跃 REQ，多 REQ 队列需架构变更
