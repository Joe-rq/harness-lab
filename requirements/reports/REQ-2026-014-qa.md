# REQ-2026-014 QA

**日期**：2026-03-29
**测试者**：AI Assistant
**状态**：✅ 通过

## 验收标准核对

| # | 标准 | 状态 | 备注 |
|---|------|------|------|
| 1 | 已有 `lint / test / build` 时自动生成 `verify` | ✅ | 真实安装验证通过 |
| 2 | 不覆盖目标项目已有真实命令 | ✅ | `package.json` 保留原脚本 |
| 3 | 缺失命令时写入统一 placeholder guard 并在报告中说明 | ✅ | 自动化测试覆盖通过 |
| 4 | README / 安装文档 / 自动化测试覆盖新行为 | ✅ | 文档与测试已同步 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
node scripts/harness-install.mjs --defaults --with-hook
```

## 执行结果

### 1. 仓库级验证

- `npm test`：通过（4 个治理回归场景）
- `npm run docs:verify`：通过
- `npm run check:governance`：通过

### 2. 真实安装回归

在临时 Git 仓库中预置以下脚本：

```json
{
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  }
}
```

执行 `node scripts/harness-install.mjs --defaults --with-hook` 后：

- ✅ 保留了原始 `lint / test / build`
- ✅ 自动写入 `verify = npm run lint && npm run test && npm run build`
- ✅ 自动补齐 `req`, `req:create`, `req:start`, `req:block`, `req:complete`
- ✅ 自动补齐 `docs:verify`, `docs:impact`, `docs:impact:json`, `check:governance`
- ✅ 复制了 `scripts/template-guard.mjs`
- ✅ 安装报告写出了 `preserved` / `generated` 状态

### 3. 缺失命令回退

自动化测试覆盖了仅存在 `test` 的场景，结果为：

- ✅ `lint` 写入 `node scripts/template-guard.mjs lint`
- ✅ `build` 写入 `node scripts/template-guard.mjs build`
- ✅ `verify` 自动降级为 `npm run test`

## 未自动验证项

- [ ] 未对非标准脚本名（如 `check`, `test:unit`, `build:prod`）做自动猜测验证

说明：这是有意不做的边界，不属于本次自动绑定策略。

## 结论

本次修复已经把“接入后真实命令绑定”的主要摩擦点收敛到可执行且可解释的状态，QA 通过。
