# REQ-2026-052 QA Report

**日期**：2026-04-28
**REQ**：REQ-2026-052 Phase 5.6 不变量清理

## 验证结果

| 验收标准 | 结果 | 证据 |
|---------|------|------|
| --scan 去重 | ✅ | 同标题+同触发路径 → 跳过，不新建 |
| --check 频率追踪 | ✅ | 触发 INV 记录到 .claude/.inv-usage.json（count + lastTriggered） |
| 90 天自动废弃 | ✅ | --scan 和 --deprecate-stale 都可触发，draft → deprecated |
| 高频升级 | ✅ | --scan 和 --upgrade-frequent 都可触发，触发 ≥10 次 → medium→high |
| npm test 全部通过 | ✅ | 11/11 PASS |
| 重复 INV 清理 | ✅ | 359 → 38（330 条重复删除 + 17 条跳过 + 9 条新候选） |

## 执行命令

```bash
# 去重扫描
node scripts/invariant-extractor.mjs --scan --dedup
# → 删除 330 条重复，跳过 17 条，提取 9 条新候选

# 频率追踪
node scripts/invariant-extractor.mjs --check --file scripts/req-check.sh
# → 6 条 INV 匹配，.inv-usage.json 记录 count+lastTriggered

# 独立废弃命令
node scripts/invariant-gate.mjs --deprecate-stale
# → 0 条废弃（所有 draft 都在 90 天内）

# 独立升级命令
node scripts/invariant-gate.mjs --upgrade-frequent
# → 0 条升级（无 INV 触发 ≥10 次）

# 治理测试
npm test
# → All governance tests passed (11).
```

## 结论

Phase 5.6 不变量清理实施完毕，不变量从 359 个膨胀文件收缩到 38 个（3 active / 34 draft / 1 deprecated），去重、频率追踪、自动废弃、高频升级四项功能全部工作正常。
