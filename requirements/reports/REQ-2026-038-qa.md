# REQ-2026-038 QA 报告

> 2026-04-25

## 验收标准验证

| # | 标准 | 结果 | 证据 |
|---|------|------|------|
| 1 | `harness-doctor` 能诊断 5 项健康指标并给出修复建议 | ✅ | 本仓库运行输出 5 项检查结果（4 pass / 1 warn / 0 fail） |
| 2 | 5 项检查：hook 配置、hook 脚本存在性、REQ 模板定制、experience 内容、不变量激活状态 | ✅ | checkHookConfig + checkHookScripts + checkReqTemplate + checkExperience + checkInvariants |
| 3 | 输出结构化诊断报告（通过/警告/失败 + 修复建议） | ✅ | 三级状态 + fix 建议 + 汇总行；--json 模式可程序消费 |
| 4 | 首个 REQ 向导能在 5 分钟内引导用户完成第一个 REQ | ⏳ | first-req.md 实现完整，需人工在 Claude Code 中运行 /first-req 验证 |
| 5 | 向导根据项目类型推荐 REQ 字段（React/Python/Go 基础识别） | ⏳ | Step 1 定义了 7 种项目类型识别信号，Step 4 定义了主题与字段映射，待人工验证 |

## 执行的命令

```bash
node scripts/harness-doctor.mjs
node scripts/harness-doctor.mjs --json
npm run harness:doctor
npm test
```

## 回归测试

- `npm test` 全部 11 个测试通过，无回归

## 发现的问题

1. **非阻塞**：first-req 向导是 Claude Code slash command，只能在交互式环境中测试。已确认 frontmatter 格式正确、内容完整。
2. **非阻塞**：harness-doctor 的 REQ 模板检查判定"占位符 > 2 为 warn"——本仓库的 REQ_TEMPLATE.md 确实包含 4 个默认占位符，这是预期行为（模板仓库保留占位符）。

## 结论

QA 基本通过。3/5 验收标准确认满足，2/5 需人工验证（slash command 交互流程）。无回归。
