# REQ-2026-015 QA

**日期**：2026-03-29
**测试者**：AI Assistant
**状态**：✅ 通过

## 验收标准核对

| # | 标准 | 状态 | 备注 |
|---|------|------|------|
| 1 | `PreToolUse` 会阻止包含模板占位符的活跃 REQ 继续写入 | ✅ | `req-check.sh` 已委托共享校验脚本；自动化测试覆盖占位符识别 |
| 2 | `PreToolUse` 会阻止 `draft` 状态的活跃 REQ 继续写入 | ✅ | 共享校验覆盖 `draft` 阻断 |
| 3 | `req:start` 会拒绝启动仍包含模板占位符的 REQ | ✅ | 自动化测试覆盖失败路径 |
| 4 | 自动化测试覆盖共享校验逻辑和 `req:start` 失败路径 | ✅ | `npm test` 通过 5 个治理回归场景 |
| 5 | 入口文档明确说明“创建 REQ 骨架”不等于“REQ 已可实施” | ✅ | README / AGENTS / CLAUDE 已同步 |

## 实际执行命令

```bash
npm run test
npm run docs:verify
npm run check:governance
```

## 执行结果

### 1. 仓库级验证

- `npm run test`：通过（5 个治理回归场景）
- `npm run docs:verify`：通过
- `npm run check:governance`：通过

### 2. 关键回归点

- ✅ 新建骨架 REQ 后，`req:start` 会因模板占位符被拒绝
- ✅ 填写真实背景、目标、验收标准后，`req:start` 可正常推进到 `in-progress`
- ✅ 共享校验同时识别模板占位符与 `draft` 状态，供 hook 路径复用
- ✅ 入口文档明确区分了“创建骨架”和“进入实施”

## 未执行项

- [ ] 未在外部接入仓库中再次做 end-to-end hook 交互录屏验证

说明：本次已验证共享校验脚本、`req:start` 失败路径和治理检查结果；外部仓库同步不并入本 REQ 验收。

## 结论

本次修复已经堵住“空模板 REQ 仍可被当成有效治理对象”的主要绕过路径，QA 通过。
