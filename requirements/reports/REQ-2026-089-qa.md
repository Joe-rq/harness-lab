# QA Report: REQ-2026-089

## 状态

- ✅ Pass

## Scope

- REQ: `requirements/in-progress/REQ-2026-089-review-plan-p0-distribution-installation.md`
- Design: `docs/plans/REQ-2026-089-design.md`
- Build / branch: 当前本地 worktree，未 commit、未 publish

## Environment

- OS / runtime: macOS Darwin arm64；Node.js `v23.6.1`；npm `10.9.2`；git `2.50.1`
- Required env: 无外部服务、无 registry 网络；npm pack 使用 `/private/tmp/harness-lab-npm-cache` 隔离缓存
- Test data: 测试在系统临时目录生成 Git 项目、合法/非法 package/settings、符号链接、active REQ 和本地 npm tarball

## 验证证据

### Commands Run

| Command | Result | Notes |
|---------|--------|-------|
| `node --check scripts/harness-install.mjs` | Pass | 安装器语法通过 |
| `node --check tests/governance.test.mjs` | Pass | 测试文件语法通过 |
| `node --test tests/governance.test.mjs` | Pass | 53/53 治理测试通过 |
| `npm test` | Pass | 97 项：governance 53、status 12、audit 11、event-store 21 |
| `npm run docs:verify` | Pass | 539 个 Markdown；diff-aware 14 个变更文件、2 条规则 |
| `npm run check:governance` | Pass | 必需文件、入口、INDEX/progress、docs、audit 全部通过；125 个历史 warning 非本次新增阻塞 |
| `npm run harness:doctor` | Pass | 7 通过、1 模板 warning、0 失败 |
| `env npm_config_cache=/private/tmp/harness-lab-npm-cache npm pack --dry-run --json --ignore-scripts` | Pass | 58 个文件；112,653 B 压缩，400,804 B 解包 |
| `git diff --check` | Pass | 无空白错误 |

## Requirement Verification

| 验收项 | 结果 | Evidence |
|--------|------|----------|
| npm 包名/bin 与 README npx 一致 | Pass | 三个公开说明面均使用 `npx --yes --package=harness-lab harness-install --defaults`；测试从本地 tgz 通过 `npm exec --package=<tgz>` 实际执行 |
| package metadata 与 tarball 隐私边界 | Pass | `type=module`、`engines.node=>=20`、逐文件 allowlist；pack/测试禁止 events、session-log、worktrees、tests、历史 REQ/report 与本机路径 |
| 默认目标 scripts 完整 | Pass | 16 个硬编码最低契约均安装；fresh fixture 通过 npm aliases 执行 status、doctor、create/start/block/experience/reflect/align/complete |
| 默认资产和运行依赖完整 | Pass | 硬编码 default target asset 清单逐文件断言；真实 tarball bin 完成完整 lifecycle |
| 重装保护用户状态 | Pass | 两次安装后 INDEX、active REQ、progress、settings 字节保持一致；真实 `req:status --json` 仍返回 `REQ-2026-777` |
| 非法配置不被覆盖 | Pass | 10 类 settings shape、5 类 package scripts shape 均在复制前失败；原文件不变且无 AGENTS/progress 落盘 |
| copy/verify 失败终态可信 | Pass | missing source 与 invalid installed file 均返回 1、报告为 partial、无“安装完成”横幅 |
| 对抗边界 | Pass | core-only、clean history、package dir/file symlink、Hook spoof/type/matcher 均有直接 fixture |

## Manual Verification

| Scenario | Result | Evidence |
|----------|--------|----------|
| 候选 tarball 文件清单人工审阅 | Pass | 58 项只包含发布入口、模板、skills、context 与运行脚本；npm 自动附带 LICENSE/package.json |
| 成功/partial 文案与退出码 | Pass | 自动化捕获 stdout、报告状态与进程状态；success 只在零失败项出现 |
| README / Claude command / source skill 同步 | Pass | 静态一致性测试要求新 npx 命令并禁止旧 `npx harness-install --defaults` |
| 浏览器/UI | N/A | 本 REQ 为 CLI/分发链路，无 UI |

## Issues

- 首次直接运行 `npm pack` 命中用户全局 npm cache 的既有权限问题；改用隔离临时 cache 后通过。未修改用户缓存，也不属于项目缺陷。
- 无阻塞性产品缺陷。跨 OS、profile-aware target gate、受管文件升级继续由已声明的 P1 工作承接。

## Meta-reflection and Alignment

- [x] 目标实现：公开入口、发布边界、fresh install、reinstall 和失败终态均有直接证据。
- [x] 旧功能保护：97 项自动化、docs 与治理门禁全部通过；用户状态 fixture 验证逐字节保留。
- [x] 逻辑正确性：补充了 core-only、符号链接、Hook 伪装、深层配置 shape 和 partial 状态对抗用例。
- [x] 完整性：README、package、installer、fixture 与三份公开说明面保持一致。
- [x] 可维护性：目标 scripts 由 installer 单一映射派生；P1 capability manifest 迁移点已记录。
- [x] 目标对齐：未实施 Hook/REQ CLI/event-store 架构改造，也未执行 publish/commit/push。
- [x] 设计对齐：实际方案与 `REQ-2026-089-design.md` 一致；新增的 preflight 深层校验属于设计中“非法配置先失败”的具体化。
- [x] 验收标准对齐：8 条验收均在上表映射到命令、fixture 或人工 tarball 清单。

## Conclusion

- Pass。所有 REQ-089 验收标准均有直接、可复现的当前状态证据。
