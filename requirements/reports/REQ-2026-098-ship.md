# Ship Report: REQ-2026-098

## Scope

- 模板仓库自身交付：投影语义、会话启动渲染、契约文档、契约测试。
- 未 publish、未打 tag；提交后随 master 推送。

## Evidence

- Code Review：Approved（同谱系自审，独立性有限，见该报告 Residual 1）。
- QA：Pass，四道门禁全绿（`npm test` 117 项、`docs:verify` 602 文件、`check:governance`、`harness:doctor` 10/3/0/1）。
- 复现证据：修复前 `lastUpdated=2026-08-13`（会话打开日）→ 修复后 `2026-07-10`（最后工作日）；本仓库实跑三区输出见 QA 报告。
- 范围门禁：临时豁免删除后，范围外写入被 `[ScopeGuard]` 拦截且未落盘。

## Release Conditions

- 发布说明需注明 `buildProgressProjection()` 的 `blockers` 字段已删除；无内部消费方（仅两处测试断言曾固化其恒空行为），但外部若复制该结构需同步。
- 发布说明需注明 `lastUpdated` 语义变化：不再由 `session_started` / `session_ended` 推进，取值可能比此前更早（此前"最后更新"实为"最后打开会话"）。
- 发布说明需注明 SessionStart 输出格式变更：`Summary:` 改为「最近事件（显示 N 条 / 共 M 条）」，并新增「机器状态」「人的笔记」两区；解析该输出的下游脚本需同步。
- 不得把 `progress.txt` 重新当作状态源；其"人的笔记"定位已写入 `AGENTS.md` 契约段与 README。

## Rollback

- `git revert` 本 REQ 的提交即可：无数据迁移、无 schema 变更、无外部副作用。
- 事件账本不受影响（本 REQ 只改投影与渲染，历史事件保持原状）。

## Conclusion

- Ready for handoff。交接链的四处缺陷已关闭并通过复现测试；`updateProgress` 两处计算、双谱系评审、legacy 事件 WARN 噪声作为后续项登记。
