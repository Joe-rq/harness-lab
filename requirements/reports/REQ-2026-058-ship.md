# Ship Report: REQ-2026-058

## 发布内容

- `scripts/worktree-utils.mjs` — worktree 检测、安全分支名、本地进度路径解析工具
- `scripts/req-cli.mjs` — 核心改造：本地进度读写、多活跃 REQ 索引管理、worktree 感知的状态查询
- `scripts/session-start.js` — 改用 `getProgressPath()` 恢复本 worktree 上下文
- `scripts/precompact-notify.mjs` — 改用 `getProgressPath()` 读取本地进度
- `scripts/session-reflect.mjs` — 改用 `getProgressPath()` 读取本地进度
- `.claude/commands/resume.md` / `self-review.md` — 命令文档同步，不再硬编码 progress.txt 路径
- `README.md` — 新增 worktree 支持章节
- `docs/plans/REQ-2026-058-design.md` — 设计文档

## 验证结果

- `npm test`：26/26 通过
- `npm run docs:verify`：通过
- `npm run check:governance`：通过

## 向后兼容性

- 主仓库（非 worktree）行为 100% 不变
- 现有 `.claude/progress.txt` 继续生效，无需迁移

## 已知限制

- worktree 的创建/删除仍需用户手动执行 `git worktree`
- hook 脚本（`session-start.sh`、`req-check.sh`）在 ship 报告中最初未覆盖，已在 Code Review 阶段识别并在 ship 前修复

## 回滚方式

删除 `.claude/worktrees/` 目录，回退 `req-cli.mjs` 等脚本到上一版本即可。
