# REQ-2026-077 QA Report

## 验收标准验证

| # | 标准 | 结果 | 证据 |
|---|------|------|------|
| AC-1 | 三入口未设置 `HARNESS_VERIFIER_MODE` 时默认 `envelope` | 通过 | `testVerifierModeDefaultsAndValidation`; `testVerifierSessionDefaultEnvelopeIsReadonlyPackage`; `testAutoReviewAndQaDefaultEnvelopeDoNotRunLegacyWork` |
| AC-2 | `legacy/envelope/subagent` 合法,非法值清晰失败 | 通过 | `testVerifierModeDefaultsAndValidation`; `testVerifierEntrypointsRejectInvalidMode` |
| AC-3 | `envelope` 生成只读 handoff package,不执行命令,不启动 `claude` | 通过 | package 含 `readonlyBoundary`;不含 fixture 源码标记 `SHOULD_NOT_APPEAR_IN_ENVELOPE`;无 legacy Markdown report |
| AC-4 | `legacy` 保持本地 report / QA 命令执行 | 通过 | `testLegacyModeStillWritesMarkdownReports` |
| AC-5 | `subagent` 显式委托 `verifier-session` 并传递 `HARNESS_VERIFIER_MODE=subagent` | 通过 | `testSubagentModeStaysExplicitAndDelegates` |
| AC-6 | 自动化测试覆盖并通过核心验证命令 | 通过 | `npm test` 通过,governance 测试 25 项通过 |

## 验证证据

| 类型 | 项目 | 结果 | 摘要 |
|------|------|------|------|
| 命令 | `node --check scripts/verifier-mode.mjs` | PASS | 语法检查通过 |
| 命令 | `node --check scripts/verifier-session.mjs` | PASS | 语法检查通过 |
| 命令 | `node --check scripts/auto-review.mjs` | PASS | 语法检查通过 |
| 命令 | `node --check scripts/auto-qa.mjs` | PASS | 语法检查通过 |
| 命令 | `node tests/governance.test.mjs` | PASS | 25 项 governance tests passed,含 6 个 verifier 新回归 |
| 命令 | `npm test` | PASS | governance 25 + req-status 12 + req-audit 11 + event-store 20 全部通过 |
| 人工/浏览器 | envelope package 内容核对 | PASS | 默认 package 只包含 REQ ID、check type、artifact path、rootDir、只读工具边界和 prompt;不包含 artifact 源码内容或 worker 推理过程 |

## 元反思检查

| 维度 | 结论 |
|------|------|
| 目标实现 | 三入口默认值已统一为 `envelope`,并补成真实 package 行为 |
| 旧功能保护 | `legacy` 模式仍可生成 Markdown review / 执行 QA 命令 |
| 逻辑正确性 | 非法模式硬失败,避免静默 fallback;subagent 仍需显式 opt-in |
| 完整性 | 默认值、合法/非法模式、envelope、legacy、subagent 五条路径均有测试 |
| 可维护性 | 模式契约集中在 `verifier-mode.mjs`,后续入口不再各自写默认字符串 |

## 对齐检查

| 维度 | 结论 |
|------|------|
| 目标对齐 | 实现直接解决 F1 默认值分裂,并为 F2 只读边界提供本地回归证据 |
| 设计对齐 | 实现与 `docs/plans/REQ-2026-077-design.md` 的模式契约一致 |
| 验收标准对齐 | AC-1 至 AC-6 均有测试或命令证据 |

## 临时实现与债务影响

- 本 REQ 未调用外部 Claude CLI;这是刻意的安全边界,不是未完成项。
- 物理 runtime 工具白名单仍沿用 REQ-066 实测证据;077 新增的是 envelope 默认路径的本地只读边界回归。
- 因 `scripts/req-check.js` 仍不识别 slugged active REQ 文件名,实施阶段使用了临时 `.claude/.req-exempt`;收尾前需删除并写入 DELETE 审计行。

## 结论

REQ-2026-077 QA 通过。
