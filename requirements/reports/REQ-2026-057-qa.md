# QA: REQ-2026-057

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | `harness-install` 支持 `--package-dir app` 和 `--package-json app/package.json`，并把治理脚本绑定到指定 package | 通过。`tests/governance.test.mjs` 覆盖 `app/package.json` 与 `api/package.json` 两种显式绑定 |
| 2 | 根目录没有 `package.json` 时，报告和终端后续步骤不再只提示不可执行的 `npm run` | 通过。缺 package 场景报告包含 `node scripts/req-cli.mjs create` fallback |
| 3 | 安装报告包含能力差距、未绑定原因和下一步信息，能解释默认安装不是完整镜像 | 通过。报告新增“能力差距与未安装项说明”，并说明高级治理脚本、测试、CI、commands 不在默认安装清单 |
| 4 | README、`.claude/commands/harness-setup.md`、`.agents/skills/source-command-harness-setup/SKILL.md` 与真实安装器行为一致 | 通过。三处均补充 `--package-dir app`、子目录 package 绑定和默认安装边界 |
| 5 | `npm test`、`npm run docs:verify`、`npm run check:governance` 通过 | 通过 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 执行结果

- `npm test`：通过（governance 14/14，req-status-json 12/12）
- `npm run docs:verify`：通过（371 markdown files checked；11 changed files across 1 triggered rule）
- `npm run check:governance`：通过

## 中途发现与处理

- 首次 `npm test` 中 `tests/req-status-json.test.mjs` 在 Windows 下生成 `D:\D:\...` 路径，导致第二组测试无法导入 `scripts/req-cli.mjs`。
- 已将该测试改为 `fileURLToPath(import.meta.url)`，完整测试链路恢复通过。

## 元反思检查

- [x] 目标实现：package 定位、报告 fallback、文档同步和测试覆盖均已完成
- [x] 旧功能保护：根目录 package 的原有绑定行为保持不变
- [x] 逻辑正确性：子目录 package 的治理命令会回到 Git 根目录执行
- [x] 完整性：缺 package、显式 package-dir、显式 package-json 均有覆盖
- [x] 可维护性：新增契约测试能发现未来入口文档和安装器行为漂移

## 对齐检查

- [x] 目标对齐：实现解决了 MediAppHub 这类根无 package / 子目录 package 的接入误导
- [x] 设计对齐：实现符合 `docs/plans/REQ-2026-057-design.md`
- [x] 验收标准对齐：所有验收标准均有实现和验证结果

## 结论

REQ-2026-057 QA 通过，可以完成。
