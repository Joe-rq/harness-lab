# REQ-2026-088 QA 报告：第二项目实验 3 缺陷修复

## 状态
- ✅ 通过

## 验证证据（三件套）
| 命令 | 结果 | 说明 |
|------|------|------|
| `npm test` | PASS | governance 43（含 REQ-088 `testExemptionHeadingLenient` + `testHarnessInstallGitignoreAndDoctor`）/ req-audit 11 / event-store 21 |
| `npm run docs:verify` | PASS | 530 markdown；diff-aware 10 changed；1 triggered rule（README 已同步）|
| `npm run check:governance` | PASS | 对齐/一致/req:audit 全过 |

## 回归矩阵
| # | 场景 | 结果 |
|---|------|------|
| T1 | 标题 `### 约束（Scope Control）`（无"，可选"）+ skip-design checkbox | hasExemption true（宽松匹配）✅ |
| T2 | fixture install → .gitignore | 含 `# Harness Lab 运行时状态` + `.claude/.req-exempt` ✅ |
| T3 | fixture install → scripts/ | 含 `harness-doctor.mjs` ✅ |
| T4 | 二次 appendGitignore | 标记段只 1 份（幂等）✅ |

## 元反思检查（verify 阶段）
- [x] **目标实现**：标题宽松 / .gitignore / doctor 传播三目标全达
- [x] **旧功能保护**：精确标题仍识别（testDesignDocExemptionMechanism 过）；npm test 全绿
- [x] **逻辑正确性**：宽松回退仅在精确取不到时触发；appendGitignore 幂等
- [x] **完整性**：三缺陷都有实现与测试
- [x] **可维护性**：.gitignore 追加幂等；doctor 复用 cli 模块清单

## 对齐检查（record 阶段）
- [x] **目标对齐**：实现服务于"降低接入摩擦"（实验驱动）
- [x] **设计对齐**：与 design 稿一致
- [x] **验收标准对齐**：四条验收全有实现与验证

## 人工/浏览器验证
- 无（fixture install + 单元测试覆盖）
