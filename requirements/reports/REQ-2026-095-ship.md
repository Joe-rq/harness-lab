# Ship Report: REQ-2026-095

## Scope

- 本地候选包与仓库交付；未 publish、未 push、未修改任何外部 worktree 项目。

## Evidence

- Review Approved；QA Pass。
- 111 tests、capability/docs/governance/doctor/health/candidate package 全部满足门禁。
- 真实 Git worktree E2E 覆盖状态隔离、冲突报告与拓扑收敛。

## Release Conditions

- 发布说明需明确 blocked/suspended 不再占 active，并说明 JSON 新增 `suspended_reqs` 的兼容影响。
- 发布说明需明确 `--all` 是只读观测，不负责协调不同 worktree。
- 不得把 runtime events/progress、session log、npm cache 或测试临时 worktree 纳入 package。

## Rollback

- 恢复旧 writer namespace、checkout-local 扫描和旧 health 计数；事件 schema 未迁移，无需删除历史账本。
- 若仅聚合异常，可撤下 `--all` 公共入口而不影响当前 checkout lifecycle。

## Conclusion

- Ready for handoff，未发布。
