# QA Report: REQ-2026-095

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| event projection | Pass | 21 tests；block → suspended，resume/completed 清除，legacy/progress fallback 保持 |
| repository semantics | Pass | examples/templates/duplicate invariants 排除；active/draft/suspended/completed 分类 |
| real Git worktree E2E | Pass | 两个 linked checkout 真实 create；identity namespace、隔离、duplicate conflict、block/session/health/doctor、remove 收敛 |
| audit regression/debt | Pass | 2 warnings / baseline 1 → regression 1、known debt 1；health fail |
| `npm test` | Pass | 111：67 governance + 12 status + 11 audit + 21 event-store |
| `npm run capabilities:check` | Pass | publication manifest in sync；state semantics 已进入 CLI capability |
| `npm run docs:verify` | Pass | 584 Markdown，71 changed files，4 rules |
| `npm run check:governance` | Pass | 125 个已知 warning，门禁通过 |
| `harness:doctor --json` | Pass | 10 pass / 3 warn / 0 fail / 1 skip；Repository state 给出统一计数与重复定位 |
| governance health | Pass | 0 regressions、125 known debt；REQ 1 active / 0 draft / 0 suspended / 1 example；Invariant 51 unique / 2 templates / 21 duplicates |
| candidate package | Pass | dry-run 72 files；公开入口和新增 `state-semantics.mjs` 在 allowlist 内；完整测试另从真实 tarball 执行安装生命周期 |

## Requirement Verification

- [x] active/draft/suspended/completed/example 与 regression/debt 有共享语义和消费者断言。
- [x] blocked/resume/completed、status/session 文本和 JSON 一致。
- [x] main/linked writer 使用真实 identity，`--all` 按真实 Git topology 只读聚合。
- [x] 双 worktree duplicate active、block 消解、默认隔离、移除收敛均为真实 Git E2E。
- [x] legacy main namespace、非 Git fixture、progress fallback 保持兼容。
- [x] README、manifest、doctor、health、候选包与完整回归对齐。

## Manual Verification

- 审阅真实 worktree E2E 的 namespace、root/branch 与冲突对象；聚合不写其他 checkout。
- 对照 README 权威表，确认 events/progress/INDEX 的职责没有交叉承诺。
- 浏览器/UI：N/A。

## Conclusion

- Pass。
