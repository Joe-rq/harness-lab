# REQ-2026-083 QA 报告：S3-CP1 exit confirmation

> 关联 REQ：`requirements/in-progress/REQ-2026-083-s3-cp1-exit-confirmation-and-weekly-data-record.md`
> QA 日期：2026-06-21
> 模式：本 REQ 为 S3-CP1 收口手续，无代码逻辑变更，QA 以证据落盘 + 治理校验 + 诚实性审查为主。

## 结论

S3-CP1 退出条件达成。周度客观数据已诚实补记、sealed 缺陷已如实记录、路线图已勾选推进。可进入 S3-CP2（REQ-B §7 填表）。

## 验证证据

### 1. 周度 data_recorded 事件补记（AC-1）

写入脚本：`/tmp/append-weekly-083.mjs`（调用 `appendEvent`，未改 `scripts/event-store.mjs`）。运行输出：

```
appended week 23: id=evt_mqnurk8b_0001_... ts=2026-06-21T13:59:07.307Z session=session-main
appended week 24: id=evt_mqnurk8d_0002_... ts=2026-06-21T13:59:07.309Z session=session-main
appended week 25: id=evt_mqnurk8d_0003_... ts=2026-06-21T13:59:07.309Z session=session-main
done: 3 events appended
```

`stats --metrics` 复核：

```
Total events: 67
  s3_observation_data_recorded: 3
```

口径自洽性：W23(42) + W24(7) + W25(4) = 53，加上 W22 边界(11) 与补记后新增的 083 生命周期事件，与窗口总量一致。每条 `payload` 含 `week_number`(number) 与 `metrics`(object)，符合 `EVENT_TYPE_SCHEMAS.s3_observation_data_recorded` schema。

诚实性：
- `ts` 全部为补记真实时刻（2026-06-21T13:59Z），未伪造为历史周。
- `metrics` 全部来自 `event-store read` 已落盘事件的真实计数。
- W25 `note` 明确标注"补记时刻快照，非最终值"（REQ-083 收口会产生 `req_completed` 使 W25 增长）。

### 2. sealed expectation 处理（AC-2）

`git diff` 证据（见 code-review 报告附录）：diff 仅落在 `## 补充说明` 区（原占位"（如需追加,写明日期和原因）"替换为 2026-06-21 声明段）。6 个主观问题正文（User 填写区 1–6）与 `## 观察后对比区` 逐字未改。

### 3. 路线图勾选（AC-3）

`multi-agent-roadmap.md`：
- 顶部"当前阶段"：`Stage 3 / S3-CP1 真实使用观察期` → `Stage 3 / S3-CP2 §7 决策评估表填写`。
- §3 S3-CP1：`[ ]` → `[x]`，补完成说明（REQ-078 启动 + REQ-083 收口）。
- §8 决策日志：新增 2026-06-21 行（诚实补记 + sealed c 处理 + 进入 S3-CP2）。

### 4. 治理校验（AC-5）

| 命令 | 结果 |
|---|---|
| `npm test` | ✅ req:status 12 / req-audit 11 / event-store 21 全过 |
| `npm run docs:verify` | ✅ 506 markdown（7 changed） |
| `npm run check:governance` | ✅ passed（warnings=125，见下） |
| `req:audit --id REQ-2026-083` | 见报告末节 |

`npm test` 中出现的 `fatal: not a git repository` 与 `Created REQ-2026-123` 是 `req:create` 测试用例在 temp dir（`/var/folders/.../req-create-id-*/`）内的预期输出，非真实失败——其后紧跟 `PASS` 与 `All req-audit tests passed (11)`。

## §7.3 反向否决自检（本 REQ 边界）

| 反向否决项 | 本 REQ 处理 |
|---|---|
| 观察期少于 2 周且无延长记录 | 观察期 5/31–6/14 已满 2 周，超期 7 天，本 REQ 记录超期事实而非伪造延长 |
| 缺少 `s3_observation_window_start` 或周度 `data_recorded` | start 已有(REQ-078)；周度 data_recorded 由本 REQ 补齐 3 条 |
| sealed 由 agent 代填或观察后补主观预测 | user 未填，agent 未代填；本 REQ 记录为协议缺陷，主观维度 N/A |
| verifier 默认模式混杂 | 窗口内 verifier_events=0（统一 envelope），数据口径一致 |
| 痛点来自文档恢复/压缩/projection/成本 | 留待 S3-CP2 / REQ-B §7.3 逐条核验 |

本 REQ 只解决"周度 data_recorded 缺失"一项；其余反向否决项如实记录为已知事实，不伪造规避。

## 已知事项 / 待 REQ-B 处理

1. **§7 维度不足**：仅 3 维度启用（failure_rate / parallel_req_count / conflict_count），且 parallel 口径脚本自标注"需补强不可信"。§7.2 要求 ≥4 维度才能做 S3-CP3 决策。根因（默认 envelope 不采 verifier 数据、无人记 human_decision）不是"补数据"能解决，需在 REQ-B 诚实记录并影响 S3-CP3 结论。
2. **governance warnings=125**：check-governance 整体 passed；125 与改动前 baseline 对比在 record 阶段核验（见下）。
3. **W25 数据非最终值**：REQ-083 收口后 W25 计数会增长，S3-CP2 填表以 `stats --metrics` 实时值为准。

## 人工验证

sealed expectation 主观区由 user 后续自行补填（本 REQ 不代填）。无其他人工验证项。
