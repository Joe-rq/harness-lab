# QA Report: REQ-2026-098

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| 复现（修复前） | Fail（预期） | `node tests/event-store.test.mjs` → `AssertionError: 打开会话不得改变"最后工作时间"`：actual `2026-08-13` vs expected `2026-07-10`；渲染层同时暴露 `TypeError: Cannot read properties of undefined (reading 'length')`（session-start.js:108 读已删字段） |
| `node tests/event-store.test.mjs` | Pass | `All event-store tests passed (25).`——21 既有 + 4 新增；新增覆盖 lastUpdated 语义、blockers 删除、三区渲染、INDEX 首项 |
| `npm test` | Pass | 117：69 governance + 12 req:status --json + 11 req-audit + 25 event-store（`PASS` 行 92 + 汇总行） |
| `npm run capabilities:check` | Pass | `Capability publication manifest is in sync.` |
| `npm run docs:verify` | Pass | `602 markdown files checked; diff-aware checked 19 changed files across 1 triggered rules.` |
| `npm run check:governance` | Pass | `Governance check passed.`（含 INDEX 与 progress.txt 一致性、docs:verify、req:audit） |
| `npm run harness:doctor` | Pass | 10 通过 / 3 警告 / 0 失败 / 1 跳过；3 条警告均为既有（模板占位符、profile 推断、repository state 的 21 组 invariant 重复），与本次改动无关 |
| 本仓库实跑 `node scripts/session-start.js` | Pass | 见下"关键输出摘要" |
| scope 门禁回归 | Pass | 删除豁免后向 `scripts/scope-probe-098.txt` 写入被 `[ScopeGuard]` 拦截且未落盘，允许范围与 REQ 声明一致 |

### 关键输出摘要（本仓库实跑，2026-09-23）

```
📊 机器状态（唯一真相源：事件账本投影）
Current active REQ: REQ-2026-098
Current phase: implementation
Last updated: 2026-09-23
Next steps: - Continue active REQ: REQ-2026-098
⏸️ 搁置中的 REQ：
  - REQ-2026-096 (blocked / qa): Claude Code 未登录，GitHub CLI token 失效且 API 不通
  - REQ-2026-097 (blocked / implementation): 三个外部项目尚未逐项授权…

🕘 最近事件（显示 8 条 / 共 152 条）

📝 人的笔记（.claude/progress.txt 原文，不参与状态判定）
最后修改：2026-09-23 ｜ CLI 会在 create/start/block/complete 时改写头部字段
（原文含 4 条 Next steps 与 2 条 Open questions，逐条出现）

📌 需求索引：## 当前搁置 REQ
- REQ-2026-097 … / - REQ-2026-096 … / - REQ-2026-901 …（三条齐全，首项未丢）
```

对照修复前同一命令的输出：`Current phase: idle` + `Last updated: 2026-08-13`（会话打开日）+ `Summary:` 中 `Active REQ: REQ-2026-095` 与 `REQ completed: REQ-2026-095` 并排 + 手写内容 0 条 + 搁置列表 3 条只显示 2 条。

## Requirement Verification

- [x] `lastUpdated` 只由工作事件推进：`req_started(2026-07-10)` + `session_started(2026-08-13)` → `2026-07-10`（新用例）。实现口径为"排除 `session_started` / `session_ended`"，非字面 `req_*` 白名单，理由见 REQ"关键决策"。
- [x] 最近事件带计数：渲染为「最近事件（显示 N 条 / 共 M 条）」，复用既有 `eventCount` + `summary.length`，未新增结构字段。
- [x] `blockers` 死字段移除：投影不再返回该字段（3 处断言），渲染分支删除；搁置原因由 `suspendedReqs[].reason` 渲染（用例断言输出含 `fixture blocker reason`）。
- [x] 「人的笔记」区原文照登：fixture sentinel 与自建小节必须出现；本仓库实跑逐条核对 4 条 Next steps + 2 条 Open questions。
- [x] INDEX 搁置列表不丢项：3 条 fixture 断言全部出现。
- [x] `AGENTS.md` 契约段（真相源权重、`lastUpdated`/`phase` 定义、三区渲染）+ 四道门禁全绿。

**验收标准第 2 条的降级说明**：条文要求"现状字段与事件流水在结构中分离"，实际在**渲染层与契约层**分离，未修改投影返回结构——嵌套结构会破坏范围外消费者（`req-cli.mjs`、`req-status-json.test.mjs`）。该取舍已记入 REQ"关键决策"与 code-review"Residual"第 3 条。

## Manual Verification

- 逐行比对本仓库「人的笔记」区与 `.claude/progress.txt` 原文，确认无改写、无截断。
- 确认输出中「机器状态」区不再出现 `Current phase: blocked`（文件里那行只出现在人的笔记区）。
- 浏览器/UI：N/A（CLI 输出，终端即最终界面）。

## Conclusion

- Pass。四项缺陷均有先失败后通过的复现测试，四道仓库门禁与 scope 门禁回归均通过。
- 未执行项：无。
