# REQ-2026-082 QA

## 状态

- 通过

## 验证证据

| 类型 | 项目 | 结果 | 摘要 |
|------|------|------|------|
| 命令 | `node tests/governance.test.mjs` | PASS | 34 项治理测试通过，包含安装器 scope-guard 配置和只读 REQ 拦截回归 |
| 命令 | `npm test` | PASS | governance、req-status-json、req-audit、event-store 全量测试通过 |
| 命令 | `npm run check:governance` | PASS | 治理结构、hook 配置一致性、R4 覆盖、docs:verify、req:audit 通过 |
| 命令 | `npm run docs:verify` | PASS | 493 个 Markdown 文件检查通过，diff-aware 覆盖 13 个变更文件 |
| 人工/浏览器 | REQ-072 写入模拟复现 | PASS | 在 MediAppHub 的 REQ-072 只读边界下模拟 Write `server/app/main.py` 返回 `decision: block`，模拟写 `requirements/reports/REQ-2026-072-audit-report.md` 返回 allow |

## 验收标准对齐

- [x] `.claude/settings.example.json` 的 Write/Edit PreToolUse 同时配置 `req-check.js` 与 `scope-guard.mjs`。
- [x] `harness-install --with-hook` 复制 `scope-guard.mjs` 并写入目标项目 hook 配置。
- [x] 安装器报告不再声明 `scope-guard` 属于默认未安装高级脚本。
- [x] 只读 REQ 阻断 `server/app/main.py`、`app/src/App.tsx`、`server/tests/test_api.py`、`Dockerfile` 写入。
- [x] 只读 REQ 允许 `requirements/reports/REQ-*.md` 报告产物写入。
- [x] 旧 REQ 无 scope 声明时仍放行。
- [x] `npm test` 通过。
- [x] `npm run check:governance` 通过。

## 元反思

- 目标实现：默认安装链路和只读拦截均有代码与测试覆盖。
- 旧功能保护：旧 REQ 无范围声明的兼容行为有回归测试。
- 逻辑正确性：只读识别限定在边界相关章节，避免背景描述误触发。
- 完整性：代码、安装报告、入口文档和 source-command skill 已同步。
- 可维护性：未引入依赖，路径解析和只读策略保持在 `scope-guard.mjs` 内部。
