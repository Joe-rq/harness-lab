# QA Report: REQ-2026-099

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| 复现（改资产前的守卫状态） | Fail（预期） | 改掉三处 npx 后，`node tests/governance.test.mjs` 报 `FAIL installer declared sources exist and CLI args are strict: README.md must publish the verified package/bin mapping` 与 `FAIL harness-setup command, skill, and bin stay aligned`——两处断言把旧 npx 写法列为必需短语 |
| `grep -rn "npx --yes --package=harness-lab" README.md .claude/commands/harness-setup.md .agents/skills/source-command-harness-setup/SKILL.md` | Pass | 无匹配（三个资产均无旧承诺） |
| `grep -n "目标支持 Windows" README.md` | Pass | 无匹配（承诺式表述已撤） |
| 三资产短语存在性（行尾锚定） | Pass | `node /path/to/harness-lab/scripts/harness-install.mjs --defaults` 与 `npm exec --yes --package=./harness-lab-<version>.tgz -- harness-install --defaults` 在 README / command / skill 三个文件中各 1 处 |
| `npm test` | Pass | 69 governance + 12 status + 11 audit + 25 event-store；退出码 0 |
| `npm run docs:verify` | Pass | 609 markdown；diff-aware 20 changed files / 1 triggered rule（`governance-automation` 因 tests 变更触发，README 已同步） |
| `npm run check:governance` | Pass | 含 INDEX ↔ progress 一致性、req:audit |
| `npm run harness:doctor` | Pass | 10 通过 / 3 警告 / 0 失败 / 1 跳过（与改动前一致） |
| `npm run req:audit` | Pass | `within baseline (125/125 warnings, no delta)` |
| `npm run req:status` | Pass | `Active REQ: REQ-2026-099 (in-progress / implementation)`；096/097 不在 active |

### 关键输出摘要

- 守卫修正前：`README.md must publish the verified package/bin mapping`（README / command / skill 三处走同一断言）。
- 守卫修正后：断言改为「源码形式 + tarball 形式必须存在」，并把 `npx --yes --package=harness-lab` 加入禁用短语表——**形式更换，强度未降**（仍是"必须存在两条 + 不得出现旧形式"）。
- 096/097 的搁置说明现在含恢复条件与"为何不做"，且已勾选验收项保留原文。

## Requirement Verification

- [x] README 无 `npx --yes --package=harness-lab`（含 upgrade / restore 示例）；包分发段两条本地路径 + "不发布到公开 registry"。
- [x] 两个发布资产同一处理；命令形式取自 `tests/governance.test.mjs` 的 packed-install fixture（唯一有运行证据的包安装形式）。
- [x] 两处守卫同步更新且未降强度；旧形式进禁用表。
- [x] 平台段列出 Windows 4 项失败与 run ID（`35858281776`、`31672675613`），明确不作承诺。
- [x] "随 npm 包发布"类措辞改为中性表述（"随包发布并由 capability manifest 声明"）。
- [x] REQ-2026-096 转 `suspended`，搁置说明含个人路线依据、实现保留、恢复条件；未勾选验收项原文保留。
- [x] REQ-2026-097 转 `suspended`，搁置说明含"原方案非必须 / 改用两项目四任务轻量观察 / 转公共路线时重写"。
- [x] 四道门禁 + `req:audit` 基线内无 delta。

**未达成项（已登记，不属本 REQ）**：文件层 `suspended` 未进事件账本，`session-start` 搁置列表仍显示 `blocked`——需要 `scripts/` 改动（事件类型或投影交叉校验），见 REQ"临时实现与债务"。

## Manual Verification

- 逐段读 README 安装段与平台段，确认无残留承诺式措辞；确认新命令形式与测试 fixture 一致（不是自造命令）。
- 读 096/097 的搁置说明，确认"为什么不做 + 什么条件下恢复 + 保留了什么"三件事齐全。
- 浏览器/UI：N/A。

## Conclusion

- Pass。承诺已收敛到有证据的形式，守卫同步收紧，两份 REQ 的状态语义与路线决策一致。
