# Ship Report: REQ-2026-093

## Scope

- 本地候选包和仓库交付；未 publish、未 push、未修改任何目标项目。

## Evidence

- Review Approved；QA Pass。
- 107 tests、capability check、docs/governance、profile-aware doctor 全部通过。
- 候选包 70 files，包含 policy 与高级 Hook；packed default install 不复制高级 Hook且真实生命周期通过。

## Rollback

- 恢复七个 Hook 的本地 mode 分支，删除 `hook-policy.mjs`。
- 删除 `.harness/profile.json` 写入与 profile-aware doctor，恢复固定诊断。
- 从 manifest publication 移除 advanced-hooks 并重新同步 package allowlist。
- 无 registry、远端或外部项目状态需要回滚。

## Conclusion

- Ready for handoff，未发布。
