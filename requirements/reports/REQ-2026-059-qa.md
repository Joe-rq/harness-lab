# QA: REQ-2026-059

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | `harness-install` 默认 CLI 清单包含 `scripts/worktree-utils.mjs` | 通过。`modules.cli.files` 已包含 `scripts/worktree-utils.mjs` |
| 2 | 迁移后的 fixture 能直接执行 `node scripts/req-cli.mjs status` | 通过。安装器测试在临时项目中实际执行该命令 |
| 3 | 迁移后的 `scripts/req-check.js` 使用 `getProgressPath()` / `getExemptPath()` | 通过。代码已复用 `worktree-utils.mjs`，并保留全局豁免兜底 |
| 4 | 安装器回归测试覆盖迁移后 CLI / hook 脚本可执行性 | 通过。fixture 实跑 `req-cli.mjs status`、`session-start.js`、`req-check.js` |
| 5 | `/harness-setup` command、source-command skill、README 与真实安装清单保持一致 | 通过。三处均补充 `worktree-utils.mjs` 或迁移后实跑说明 |
| 6 | `npm test`、`npm run docs:verify`、`npm run check:governance` 全部通过 | 通过 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 执行结果

- `npm test`：通过（governance 14/14，req-status-json 12/12）
- `npm run docs:verify`：通过（402 markdown files checked；13 changed files across 1 triggered rule）
- `npm run check:governance`：通过

## 中途发现与处理

- 首次 `docs:verify` 因修改 `tests/governance.test.mjs` 触发 `governance-automation` 文档同步规则失败。
- 已更新 `README.md`，说明迁移默认 CLI 清单包含 `worktree-utils.mjs`，并明确安装器测试会在迁移后的 fixture 中实跑 CLI / hook 入口。

## 元反思检查

- [x] 目标实现：迁移命令已包含 worktree 运行时依赖，跨平台 hook 已路径感知
- [x] 旧功能保护：默认安装、package 绑定和现有 req:status 测试均通过
- [x] 逻辑正确性：修复针对迁移清单缺依赖与 `req-check.js` 硬编码根因
- [x] 完整性：CLI 入口、SessionStart hook、PreToolUse hook 均有迁移后实跑覆盖
- [x] 可维护性：路径逻辑集中复用 `worktree-utils.mjs`

## 对齐检查

- [x] 目标对齐：修复只针对迁移命令未完整包含 REQ-2026-058 worktree 能力
- [x] 验收标准对齐：所有 6 条验收标准均已满足

## 结论

REQ-2026-059 QA 通过，可以完成。
