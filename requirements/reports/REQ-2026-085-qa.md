# REQ-2026-085 QA 报告：OPT-1A — req-check stdin 契约 + Bash 写入门禁

## 状态
- ✅ 通过

## 验证证据

### 自动化测试（三件套）
| 命令 | 结果 | 说明 |
|------|------|------|
| `npm test` | PASS | governance 38（含 OPT-1A 新增 4） / req-status-json / req-audit 11 / event-store 21 全过 |
| `npm run docs:verify` | PASS | 516 markdown；diff-aware 12 changed；1 triggered rule（governance-automation，CONTRIBUTING 已同步）|
| `npm run check:governance` | PASS | README/CLAUDE 对齐、INDEX/progress 一致、docs:verify 过、req:audit 过（125 legacy warnings 为基线，非新增）|

### OPT-1A 回归矩阵（tests/governance.test.mjs）
| # | 场景 | 结果 |
|---|------|------|
| T1 | 无 REQ + Bash `echo x > src/a.ts`（重定向写）| exit 2 阻断 ✅ |
| T2 | 无 REQ + Bash `sed -i 's/a/b/' src/a.ts`（原地改）| exit 2 阻断 ✅ |
| T3 | 无 REQ + Bash `ls` / `grep` / `cat` / `git status`（纯读）| exit 0 放行 ✅ |
| T4 | 无 REQ + Write `requirements/in-progress/REQ-x.md` | exit 0 白名单放行 ✅（死代码修复）|
| T5 | 无 REQ + Write `docs/plans/x.md` / `.claude/progress.txt` | exit 0 白名单放行 ✅ |
| T6 | 无 REQ + Bash `echo x > .claude/state.json` | exit 0 白名单放行 ✅ |
| T7 | 有 REQ（范围 `scripts/foo.mjs`）+ Bash `echo > scripts/foo.mjs` | scope-guard 放行 ✅ |
| T8 | 有 REQ + Bash `echo > scripts/bar.mjs`（范围外）| scope-guard 阻断 ✅ |
| 既有 | slugged active REQ（in-progress）+ Write | 放行（修正后喂 stdin）✅ |
| 既有 | slugged active REQ（draft）+ Write | exit 2 阻断 ✅ |

### 平台缺口（不可强制，已记录；文档声明归 REQ-2026-086）
- subagent 工具调用不触发 PreToolUse（GitHub #21460 / #34692）
- `claude -p` 非交互不触发（#40506）
- `perl -e` / `python -c` 等解释器写（理论不可封）

## 元反思检查（verify 阶段）
- [x] **目标实现**：stdin 分流 / matcher 扩面 / scope-guard Bash / 测试，四目标全达
- [x] **旧功能保护**：Write/Edit 既有 REQ 门禁不变（slugged + `testLocalHookConfigUsesExistingJsEntrypoints` 过）
- [x] **逻辑正确性**：Bash 启发式对 `ls`/`grep`/`cat`/`git status` 放行（T3）；白名单路径边界正确（T4-T6）
- [x] **完整性**：四类验收场景（写拦 / 读放行 / 白名单 / 范围外）均有实现 + 测试
- [x] **可维护性**：Bash 检测集中在 `classifyBashCommand`（req-check.js + scope-guard.mjs 内联一致；REQ-086 若漂移抽共享模块）

## 对齐检查（record 阶段）
- [x] **目标对齐**：实现服务于"门禁覆盖 Bash + 修白名单死代码"
- [x] **设计对齐**：与 design 稿一致；范围从声明 4 文件扩到 7 文件（matcher 三处同步 + docs-sync 强制 CONTRIBUTING），已在 REQ 范围段诚实记录原因
- [x] **验收标准对齐**：五条验收全有实现 + 验证

## 人工/浏览器验证
- 无（CLI 工具，无 UI；行为由单元测试通过 `runReqCheck` / `runScopeGuardRaw` 喂 stdin 直接覆盖）
