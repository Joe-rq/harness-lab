# QA: REQ-2026-058

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | 在 git worktree 中执行 req:start 能成功启动 REQ，进度写入 .claude/worktrees/{branch}/progress.txt | 通过。`req-cli.mjs` 的 `startCommand()` 调用 `getProgressPath()` 获取本地路径，通过 `ensureWorktreeDir()` 创建目录 |
| 2 | 在 git worktree 中 req:status 能正确读取本 worktree 的活跃 REQ | 通过。`statusCommand()` 默认读取 `getProgressPath(root)` 对应文件 |
| 3 | 多个 worktree 可同时有活跃 REQ，INDEX.md 正确列出所有活跃 REQ | 通过。`updateIndex()` 改为列表追加，`removeActiveId` 按文件去重，允许多个 REQ 同时活跃 |
| 4 | 非 worktree 环境（主仓库直接工作）行为与改动前完全一致 | 通过。`getWorktreeId()` 返回 `null` 时路径回退到 `.claude/progress.txt`；npm test 全部通过 |
| 5 | session-start 在 worktree 中启动时能恢复本 worktree 的 REQ 上下文 | 通过。`session-start.js` 已改用 `getProgressPath(rootDir)` |
| 6 | PreCompact hook 和 SessionEnd hook 在 worktree 中读取正确的 progress.txt | 通过。`precompact-notify.mjs` 和 `session-reflect.mjs` 已改用 `getProgressPath(rootDir)` |
| 7 | npm test 通过，npm run docs:verify 通过 | 通过。26/26 tests passing，docs:verify 通过，check:governance 通过 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 执行结果

- `npm test`：通过（26 tests passing，含 req-status-json 12/12 和 governance 14/14）
- `npm run docs:verify`：通过
- `npm run check:governance`：通过

## 中途发现与处理

- `req:start` 最初因 REQ 模板缺少 `(可选)` 后缀被 `req-validation.mjs` 拒绝。已修正 REQ 文件。
- `npm test` 最初因 `toFullPath()` 未处理绝对路径导致 progress 文件写入错误位置。已添加 `path.isAbsolute()` 防护。
- `req:status --json` 测试在无活跃 REQ 场景下未读取 external mappings，导致断言失败。已修复 `statusCommand()` json 分支。

## 元反思检查

- [x] 目标实现：worktree 检测 + 本地进度隔离完整
- [x] 旧功能保护：主仓库模式完全不受影响
- [x] 逻辑正确性：分支名安全化、路径处理边界正确
- [x] 完整性：所有读取 progress.txt 的入口已改造（req-cli、session-start、precompact-notify、session-reflect）
- [x] 可维护性：worktree 工具函数集中在 `worktree-utils.mjs`，被多处复用

## 对齐检查

- [x] 目标对齐：实现解决了 worktree 并行推进 REQ 的问题
- [x] 设计对齐：实现符合方案 A（本地隔离）
- [x] 验收标准对齐：所有 7 条验收标准均已满足

## 结论

REQ-2026-058 QA 通过，可以完成。
