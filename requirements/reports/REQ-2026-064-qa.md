# QA: REQ-2026-064

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | incremental scan 能识别 `experience/foo.md` 和 `context/experience/foo.md` 来源 | 通过 |
| 2 | 已处理 experience 不再重复生成新的 invariant | 通过 |
| 3 | 新 experience 仍能生成新的 draft invariant | 通过 |
| 4 | 回归测试通过 | 通过 |
| 5 | `npm test`、`docs:verify`、`check:governance` 通过 | 通过 |

## 验证证据

| 类型 | 项目 | 结果 | 摘要 |
|------|------|------|------|
| 命令 | `npm test` | PASS | 新增 `invariant incremental scan skips processed sources`，治理测试增至 15 个 |
| 命令 | `npm run docs:verify` | PASS | 文档同步检查通过 |
| 命令 | `npm run check:governance` | PASS | governance audit 仅有 legacy warning，无 error |
| 人工/浏览器 | git status 噪音核对 | PASS | 清理本次自动生成的重复 draft invariant 后，未再出现 INV-102..117 重复候选 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 临时实现与债务影响

- 本 REQ 未引入 mock、fallback 或临时兼容债务。

## 元反思检查

- [x] 目标实现：incremental 来源去重已修复。
- [x] 旧功能保护：新 experience 仍可生成候选。
- [x] 逻辑正确性：修复来源归一化根因。
- [x] 完整性：覆盖两种来源写法。
- [x] 可维护性：解析逻辑集中。

## 对齐检查

- [x] 目标对齐：修复只针对重复生成 invariant 的 bug。
- [x] 验收标准对齐：所有验收标准均通过。

## 结论

REQ-2026-064 QA 通过，可以完成。
