# REQ-2026-016 QA

**日期**：2026-03-29
**测试者**：AI Assistant
**状态**：✅ 通过

## 验收标准核对

| # | 标准 | 状态 | 备注 |
|---|------|------|------|
| 1 | 安装器会复制当前必需脚本 | ✅ | 自动化测试确认复制 `req-validation.mjs`、`req-check.sh` |
| 2 | `--with-hook` 生成 `command` 型硬阻断 hooks | ✅ | 自动化测试确认 `SessionStart` / `PreToolUse` 结构与命令内容 |
| 3 | 安装报告和终端后续步骤说明“先写实 REQ 再 start” | ✅ | 报告内容和 CLI 输出已同步 |
| 4 | `/harness-setup` 文档同步硬阻断和骨架 REQ 约束 | ✅ | 文档已更新 |
| 5 | 自动化测试覆盖新的安装结果和 hook 配置 | ✅ | `npm test` 通过 5 个治理回归场景 |

## 实际执行命令

```bash
npm run test
npm run docs:verify
npm run check:governance
```

## 执行结果

### 1. 仓库级验证

- `npm run test`：通过（5 个治理回归场景）
- `npm run docs:verify`：通过
- `npm run check:governance`：通过

### 2. 关键回归点

- ✅ 安装器默认 CLI 模块包含 `req-validation.mjs`
- ✅ hooks 模块包含 `req-check.sh`
- ✅ `settings.local.json` 中的 `PreToolUse` 与 `SessionStart` 都是 `command` hooks
- ✅ 接入报告明确写出 PreToolUse 为硬阻断，以及 `req:create` 只生成骨架
- ✅ 临时项目的 `progress.txt` 会提示“写实 REQ 后再执行 req:start”

## 未执行项

- [ ] 未在真实外部业务仓库中再次做人工接入演练

说明：本次已在自动化测试里验证安装结果和接入报告；外部仓库演练不并入当前 REQ 验收。

## 结论

一键接入路径已经重新对齐到当前仓库的硬阻断治理契约，QA 通过。
