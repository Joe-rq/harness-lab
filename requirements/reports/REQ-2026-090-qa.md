# QA Report: REQ-2026-090

## 状态

- ✅ Pass

## Scope

- REQ: `requirements/in-progress/REQ-2026-090-p0-canonical-path-multi-target-guard.md`
- Design: `docs/plans/REQ-2026-090-design.md`
- Build / branch: 当前本地 worktree，未 commit、未 publish

## Environment

- OS / runtime: macOS Darwin arm64；Node.js `v23.6.1`；npm `10.9.2`；git `2.50.1`
- Required env: 无外部服务、无 registry 网络；npm pack 使用 `/private/tmp/harness-lab-npm-cache` 隔离缓存
- Test data: 系统临时目录内的仓库、嵌套 cwd、非法 cwd、符号链接、真实 git worktree、active REQ 与 global/worktree exemption

## 验证证据

### Commands Run

| Command | Result | Notes |
|---------|--------|-------|
| `node --check scripts/write-target-policy.mjs` | Pass | 共享策略语法通过 |
| `node --check scripts/req-check.js` | Pass | REQ 门禁语法通过 |
| `node --check scripts/scope-guard.mjs` | Pass | scope 门禁语法通过 |
| `node --test tests/governance.test.mjs` | Pass | 58/58 治理测试通过 |
| `npm test` | Pass | 102 项：governance 58、status 12、audit 11、event-store 21 |
| `npm run docs:verify` | Pass | 546 个 Markdown；diff-aware 24 个变更文件、2 条规则 |
| `npm run check:governance` | Pass | 全部必需门禁通过；125 个历史 warning 非本次新增阻塞 |
| `npm run harness:doctor` | Pass | 7 通过、1 模板 warning、0 失败 |
| `env npm_config_cache=/private/tmp/harness-lab-npm-cache npm pack --dry-run --json --ignore-scripts` | Pass | 59 个文件；115,467 B 压缩，413,454 B 解包；包含共享策略模块 |
| `git diff --check` | Pass | 无空白错误 |

## Requirement Verification

| 验收项 | 结果 | Evidence |
|--------|------|----------|
| 共享分类与 canonicalization | Pass | 两个 Hook 均 import `write-target-policy.mjs`；旧 classifier 已移除 |
| 路径归一化与 containment | Pass | `.`、`..`、反斜杠、绝对路径、prefix collision、symlink ancestor 均有 fixture |
| 全写目标识别 | Pass | redirect、tee、rm/touch/mkdir、cp/mv/ln、sed/perl/gawk inplace、复合命令均断言完整目标集 |
| req-check 全目标白名单 | Pass | 全治理目标放行；治理路径与业务路径混合时回到正常 REQ 门禁 |
| scope fail-closed / legacy compatible | Pass | unresolved、越界、scope 不匹配阻断；纯读和无 scope 旧 REQ 保持允许 |
| exemption | Pass | global 与真实 worktree `.req-exempt` 均有直接 fixture |
| 分发完整性 | Pass | npm allowlist、安装 manifest、硬编码默认资产契约和 pack 清单均包含共享模块 |
| 对抗矩阵 | Pass | traversal、prefix、Windows separator、symlink、混合目标、动态目标、无效 cwd 与控制字符日志路径均覆盖 |

## Issues

- 无阻塞性产品缺陷。
- tokenizer 的能力边界和平台无法强制的缺口已在 README、REQ 与评审报告中一致披露，不计为本 REQ 未完成项。

## Manual Verification

| Scenario | Result | Evidence |
|----------|--------|----------|
| README 支持模式与歧义策略审阅 | Pass | 文档只声明已覆盖的 redirect/tee/文件操作/inplace 模式；动态目标在显式 scope 下阻断，无 scope legacy 兼容，解释器写与平台缺口明确披露 |
| 多目标阻断原因可定位性 | Pass | 对 scope 失败输出逐目标路径与原因；日志中的 CR/LF/tab 已清理，混合目标和 unresolved fixture 验证不会只报告首个合法目标 |
| 浏览器/UI | N/A | 本 REQ 为 CLI Hook 与文件路径策略，无浏览器或图形界面 |

## Meta-reflection and Alignment

- [x] 目标实现：共享 policy、全目标判断和 canonicalization 均有直接证据。
- [x] 旧功能保护：纯读、无 scope legacy、治理目录引导和两类豁免无回归。
- [x] 逻辑正确性：支持命令的 source/destination 语义有独立断言；歧义不伪装成纯读。
- [x] 完整性：源码、安装 manifest、npm allowlist、README、fixture 已同步。
- [x] 可维护性：两个 Hook 仅消费结构化 `targets/unresolved`，不再复制 classifier。
- [x] 目标对齐：只收口 P0 路径门禁，没有提前改造 P1 mode/profile 架构。
- [x] 设计对齐：实现符合设计稿的 tokenize → canonicalize → all-target decision 流程。
- [x] 验收标准对齐：9 条验收均映射到命令、fixture 或 pack 证据。

## Conclusion

- Pass。REQ-090 的所有验收标准均有当前树上的可复现证据。
