# QA Report: REQ-2026-091

## 状态

- ✅ Pass

## Scope

- REQ: `requirements/in-progress/REQ-2026-091-p0-executable-user-docs.md`
- Design: `docs/plans/REQ-2026-091-design.md`
- Build / branch: 当前本地 worktree，未 commit、未 publish

## Environment

- OS / runtime: macOS Darwin arm64；Node.js `v23.6.1`；npm `10.9.2`；git `2.50.1`
- Required env: 无外部服务和 registry 网络；pack 使用隔离临时 npm cache
- Test data: 临时 Git 项目、本地候选 tarball、中文标题、合法与五类非法 slug、安装基线和完整 REQ 生命周期

## 验证证据

### Commands Run

| Command | Result | Notes |
|---------|--------|-------|
| `node --check scripts/req-cli.mjs` | Pass | CLI 语法通过 |
| `node --check tests/governance.test.mjs` | Pass | fixture 语法通过 |
| `node --test tests/governance.test.mjs` | Pass | 60/60 治理测试通过 |
| `npm test` | Pass | 104 项：governance 60、status 12、audit 11、event-store 21 |
| `npm run docs:impact` | Pass | 12 个变更文件触发 3 条规则，均由 README 同步满足 |
| `npm run docs:verify` | Pass | 553 个 Markdown；diff-aware 检查 12 个变更文件、3 条规则 |
| `npm run check:governance` | Pass | 必需门禁通过；125 个历史 warning 未增长为当前阻塞 |
| `npm run harness:doctor` | Pass | 7 通过、1 模板 warning、0 失败 |
| `env npm_config_cache=/private/tmp/harness-lab-npm-cache npm pack --dry-run --json --ignore-scripts` | Pass | 59 个文件；117,819 B 压缩，419,760 B 解包 |
| `git diff --check` | Pass | 无空白错误 |

## Requirement Verification

| 验收项 | 结果 | Evidence |
|--------|------|----------|
| 中文 create 与严格 slug | Pass | 中文标题生成 `REQ-2099-010-requirement.md`；合法 `login-fix-2` 可用；traversal、空白、大小写、中文、双连字符均在写入前失败 |
| 中文 experience 文件名 | Pass | 纯中文标题生成 `REQ-2099-010-experience.md`，无空尾缀或路径字符 |
| first-req 双入口 | Pass | skill 明确 npm alias / direct Node 两种入口；packed target 实际执行 direct Node status/create 与 npm lifecycle aliases |
| 技术栈中立验证 | Pass | 三类 REQ builder 无硬编码 `npm test`；模板要求真实执行；testing strategy 覆盖 JS/Python/Go/Rust/Generic 条件候选 |
| mode/profile 事实 | Pass | README 覆盖 req-check、scope、deploy、review、risk、watchdog、stop、precompact；没有修改 Hook 源码 |
| README 公开旅程 | Pass | 本地 tarball 离线 npm exec/install 后，建立安装基线并执行中文 create、start、status、block、resume、experience、reflect、align、默认 complete、doctor |
| 漂移保护 | Pass | 静态契约锁定 npx、block 完整参数、resume、中文、mode 事实、双入口、多生态与无硬编码 npm；运行 fixture 锁定真实可执行性 |

## Manual Verification

| Scenario | Result | Evidence |
|----------|--------|----------|
| first-req JS / 非 JS 路径走读 | Pass | npm alias 存在时使用 alias；无 package alias 但 CLI 已安装时使用 direct Node；两者都缺失才要求 setup |
| mode/profile 与源码逐项核对 | Pass | `rg harness-mode scripts` 返回 7 个读取 mode 的高级/基础脚本，README 表逐一覆盖；req-check 明确不读取 mode |
| README 首次旅程可理解性 | Pass | 安装审阅/独立基线 → 中文 create → 写实内容 → 生命周期；解释了为何不能用 `--no-docs-gate` 掩盖安装 diff |
| 浏览器/UI | N/A | 本 REQ 为 CLI、Markdown skill 和文档契约，无图形 UI |

## Issues

- 首次将 packed fixture 的 complete 改为默认参数后，文档门禁因未归档安装 diff 正确阻断。补充独立安装基线步骤后通过；这是用户旅程缺失，不是测试绕过。
- 无阻塞性产品缺陷。

## Meta-reflection and Alignment

- [x] 目标实现：中文 create/experience、技术栈中立、mode 事实与命令 fixture 均有直接证据。
- [x] 旧功能保护：英文/显式 slug、现有 lifecycle、installer 与 Hook 行为无回归。
- [x] 逻辑正确性：fallback 由 REQ ID 保证唯一且不可形成路径；示例与真实命令边界明确。
- [x] 完整性：CLI、skill、模板、testing context、README、fixture 已同步。
- [x] 可维护性：公开命令契约集中在 packed fixture；P1 mode/capability 边界已记录。
- [x] 目标对齐：只完成评审的用户可执行文档 P0，没有改造 Hook 状态机。
- [x] 设计对齐：安全 fallback、真实命令优先、逐 Hook 事实表均按设计实现。
- [x] 验收标准对齐：8 条验收均映射到命令、fixture 或人工走读。

## Conclusion

- Pass。REQ-091 所有验收标准均有当前树上的可复现证据。
