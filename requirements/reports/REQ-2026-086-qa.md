# REQ-2026-086 QA 报告：OPT-1B — 目标项目 matcher 传播 + 缺口声明 + doctor 自检

## 状态
- ✅ 通过

## 验证证据（四件套）
| 命令 | 结果 | 说明 |
|------|------|------|
| `npm test` | PASS | governance 40（含 OPT-1B 新增 2 + install matcher 断言）/ req-audit 11 / event-store 21 |
| `npm run docs:verify` | PASS | 520 markdown；diff-aware 10 changed；1 triggered rule（governance-automation，README 已同步）|
| `npm run check:governance` | PASS | README/CLAUDE 对齐、INDEX/progress 一致、req:audit 过 |
| `npm run harness:doctor` | 7 通过 \| 1 警告 \| 0 失败 | 三项新检查全 ✅；1 警告是 REQ 模板占位符（pre-existing，模板本就该留占位符给用户填）|

## OPT-1B 回归矩阵
| # | 场景 | 结果 |
|---|------|------|
| T1 | `harness-install` fixture → 生成 settings PreToolUse matcher 含 `Bash` | ✅（`testHarnessInstallArtifacts` 新增断言）|
| T2 | README 含三类不可强制关键词（subagent / `claude -p` / 解释器）| ✅（`testReadmeDeclaresUnenforceableGaps`）|
| T3 | doctor 源含三检查函数 + `--json` 输出含三项 name | ✅（`testDoctorIncludesOpt1Checks`）|
| T4 | `harness:doctor` 实际输出 | Bash 覆盖 ✅ / stdin 契约 ✅ / 平台缺口 ✅ |

## 元反思检查（verify 阶段）
- [x] **目标实现**：install matcher 传播 / 文档声明 / doctor 自检三目标全达
- [x] **旧功能保护**：install 其他行为不变（`testHarnessInstallArtifacts` 其余断言全过）；npm test 全绿
- [x] **逻辑正确性**：doctor self-test 真验证 stdin（`spawnSync` req-check 喂 `ls` → exit 0），非占位
- [x] **完整性**：三类缺口都有文档（README + AGENTS）+ doctor 提示
- [x] **可维护性**：doctor 检查项复用既有 `{name,status,detail,fix}` 结构，集中可扩展

## 对齐检查（record 阶段）
- [x] **目标对齐**：实现服务于"目标项目 Bash 门禁 + 缺口诚实 + 可检测"
- [x] **设计对齐**：与 design 稿一致
- [x] **验收标准对齐**：五条验收全有实现与验证

## 临时债务
- 无。④ hook 输出格式 / ⑤ `.mjs` 后缀改名为显式缓做的**非目标**（见 REQ 非目标段 + 关键决策），非临时实现债务，不影响本 REQ 验收。

## 人工/浏览器验证
- 无（`harness:doctor` 即端到端自验证：matcher 覆盖检查读真实 settings、stdin self-test 跑真实 req-check）
