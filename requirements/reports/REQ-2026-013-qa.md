# REQ-2026-013 QA

**日期**：2026-03-29
**测试者**：AI Assistant
**状态**：✅ 通过

## 验收标准核对

| # | 标准 | 状态 | 备注 |
|---|------|------|------|
| 1 | `npm test` 可运行并覆盖核心回归场景 | ✅ | 真实执行通过 |
| 2 | workflow 在 push / pull request 上运行三道门禁 | ✅ | YAML 已声明，静态核对通过 |
| 3 | 入口文档明确仓库级自动化验证入口 | ✅ | README / CONTRIBUTING / CLAUDE 已更新 |
| 4 | 治理自检约束测试入口和 workflow 存在 | ✅ | `npm run check:governance` 通过 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 执行结果

### 1. 仓库级自动化测试

`npm test` 输出结果：

- ✅ `docs verify passes on the repository`
- ✅ `req-cli lifecycle works in a fixture repository`
- ✅ `harness-install copies governance files and writes hook config`

### 2. 治理自检

- `npm run docs:verify`：通过
- `npm run check:governance`：通过

### 3. CI 配置核对

`.github/workflows/governance.yml` 已覆盖：

- ✅ `push`
- ✅ `pull_request`
- ✅ `npm test`
- ✅ `npm run docs:verify`
- ✅ `npm run check:governance`

## 未自动验证项

- [ ] 未在真实 GitHub Actions runner 上执行 workflow

说明：本地已完成 workflow 静态核对，但未触发远端 CI 运行。

## 结论

本次改动已经补齐“仓库级自动化测试 + CI 门禁”的最小闭环，QA 通过。
