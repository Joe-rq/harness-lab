# REQ-2026-079 QA Report

## 验收标准验证

| # | 标准 | 结果 | 证据 |
|---|------|------|------|
| AC-1 | `.claude/settings.local.json` 使用现存 `.js` hook，commit-msg hook 指向当前仓库脚本 | 通过 | governance 回归 `local hook config uses existing JS entrypoints`；`ls -l .git/hooks/commit-msg` 显示 `../../scripts/commit-msg-check.sh` |
| AC-2 | `req-check.js` 能通过 slugged active REQ fixture，并继续阻断 draft/template REQ | 通过 | `testReqCheckAcceptsSluggedActiveReq`；当前仓库 `node scripts/req-check.js` exit 0 |
| AC-3 | event-store 同月重复 rotation 后 `readEvents` 不再返回重复事件 | 通过 | `testRotationDoesNotDuplicateWhenArchiveAlreadyExists`；event-store tests 21 项通过 |
| AC-4 | `auto-review.mjs` shell 文件检查不再通过 shell 字符串拼接文件名 | 通过 | `testAutoReviewUsesArgArrayForShellSyntaxCheck` |
| AC-5 | README 不再声称“约 1500 行” | 通过 | `rg -n "1500" README.md` 无匹配 |
| AC-6 | 核心验证链通过 | 通过 | `npm test`、docs verify、governance check 均通过 |

## 验证证据

| 类型 | 命令 | 结果 | 摘要 |
|------|------|------|------|
| 语法 | `node --check scripts/req-check.js` | PASS | 无语法错误 |
| 语法 | `node --check scripts/event-store.mjs` | PASS | 无语法错误 |
| 语法 | `node --check scripts/auto-review.mjs` | PASS | 无语法错误 |
| 针对性 | `node tests/event-store.test.mjs` | PASS | All event-store tests passed (21) |
| 针对性 | `node tests/governance.test.mjs` | PASS | All governance tests passed (28) |
| hook | `node scripts/req-check.js` | PASS | 当前 slugged active REQ 可通过；Node 输出 MODULE_TYPELESS_PACKAGE_JSON warning，不影响 exit 0 |
| 全量 | `npm test` | PASS | governance 28 + req-status 12 + req-audit 11 + event-store 21 全部通过 |
| 文档 | `node scripts/docs-verify.mjs --no-diff-aware` | PASS | 483 markdown files checked |
| 门禁 | `node scripts/check-governance.mjs --no-diff-aware` | PASS | Governance check passed；仍有 125 legacy warnings |

## 元反思检查

| 维度 | 结论 |
|------|------|
| 目标实现 | P0/P1 中纳入本 REQ 的 hook、lookup、rotation、auto-review 和 README 口径均已修复 |
| 旧功能保护 | 现有测试全量通过；event-store archive 可读性保留 |
| 逻辑正确性 | 修复针对根因：文件名契约不一致、归档后未清空、shell 字符串拼接、断链 hook |
| 完整性 | 新增覆盖真实失败路径的回归测试；commit-msg symlink 也已人工核对 |
| 可维护性 | 改动小且局部，未引入新依赖或跨模块重构 |

## 对齐检查

| 维度 | 结论 |
|------|------|
| 目标对齐 | 未清理权限表、未拆 `req-cli.mjs`、未清 invariants，符合非目标 |
| 验收标准对齐 | AC-1 至 AC-6 均有命令或测试证据 |

## 临时实现与债务影响

- `.claude/settings.local.json` 的 allow 权限表仍含历史 `.sh`、旧路径、curl/echo 记录；本 REQ 只修实际 hook command，权限表清理需单独处理。
- `.git/hooks/commit-msg` 是本地 Git hook，不随 Git 提交分发；后续可考虑由安装器或 doctor 检查当前仓库 hook 状态。

## 结论

REQ-2026-079 QA 通过。

<!-- Source file: REQ-2026-079-qa.md -->
