# Ship Report: REQ-2026-099

## Scope

- 模板仓库自身的对外承诺收敛（文档 + 一处测试契约 + 两份 REQ 状态）；未 publish、未打 tag。

## Evidence

- Code Review：Approved（同谱系自审，独立性有限）。
- QA：Pass，四道门禁 + `req:audit` 基线内无 delta；三资产短语检查均由命令输出留证。
- 路线依据：`docs/plans/2026-09-23-route-decision-personal-slim-tool.md`（§5 矩阵 B 的 P02 / P12 / P03）。

## Release Conditions

- 发布说明需注明：**本仓库不发布到公开 registry**，安装只支持源码目录与本地 tarball；文档与两个随包资产里的命令已统一为 `npm exec --yes --package=<tarball> -- harness-install`（与 packed-install fixture 同形式）。
- 发布说明需注明：Windows 未验证、不作承诺，并引用失败 run `35858281776` / `31672675613` 与 4 项失败点；如果今后要恢复平台承诺，先取得三平台成功 run identity。
- 发布说明需注明：REQ-2026-096 / 097 转 `suspended`（个人路线暂停三平台/matcher 承诺、原 Pilot 方案非必须），实现与 collector 仍随包发布。
- 不得把 `blocked` 与 `suspended` 混用：前者是欠账，后者是已决定不做。

## Rollback

- `git revert` 本 REQ 提交，并把 096/097 的状态改回 `blocked`。
- 无代码、无数据迁移、无外部副作用（未动 registry、未打 tag、未改 CI）。

## Conclusion

- Ready for handoff。两条已知为假的公开承诺已撤下并加了防回流守卫；ROUTE 决策的 Day 0–30 第一项完成。剩余 Day 0–30 项：D2 fail-closed、functional-core、complete 门槛提前披露、两个自有项目的四任务观察。
