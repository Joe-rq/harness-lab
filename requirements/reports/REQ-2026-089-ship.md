# Ship Report: REQ-2026-089

## Scope

- REQ: `REQ-2026-089`
- Release target: `harness-lab@1.1.0` 本地候选 tarball与仓库交付物
- Operator: Codex（本地验证与交付准备）

## Preconditions

- Review: Approved，见 `requirements/reports/REQ-2026-089-code-review.md`
- QA: Pass，见 `requirements/reports/REQ-2026-089-qa.md`
- Config / docs updated: `package.json`、README、Claude command、source-command skill 已同步
- Scope note: REQ 明确不执行 npm publish、Git commit 或 push

## Commands and Actions

| Step | Result | Evidence |
|------|--------|----------|
| Syntax / tests | Pass | `npm test` 共 97 项通过 |
| Governance gates | Pass | `docs:verify`、`check:governance`、doctor 均通过 |
| Package build | Pass | `npm pack --dry-run --json --ignore-scripts`：58 files，112,653 B |
| Package smoke | Pass | 测试内从真实 tgz 执行离线 npm exec、真实 `.bin/harness-install` 和完整 REQ 生命周期 |
| Publish / deploy | Not performed | 不在 REQ 授权范围；未修改 registry 或远端仓库 |

## Risks

- 当前证据覆盖本地候选 tarball，不代表 npm registry 已存在同内容版本；正式发布需按发布时点重新运行相同门禁。
- 本轮仅在 Darwin arm64 实机验证；多 OS CI 是后续 P1 交付。
- 目标项目治理规则的 profile 适配、升级所有权和原子写入仍按 P1 计划推进，不影响本 REQ 的 fresh-install 契约结论。

## Rollback

- 回滚入口：还原本 REQ 六个实现文件；本轮没有数据库、远端或 registry 状态。
- 回滚步骤：恢复 `package.json` 发布元数据、安装器、测试和三个公开说明面至变更前版本。
- 回滚后验证：运行 `npm test && npm run docs:verify && npm run check:governance`，并确认旧安装入口已从所有说明面一致恢复。

## Conclusion

- Ready for handoff，未发布。候选包和安装链路满足 REQ-089 发布准备条件；任何真实 publish 仍需用户另行授权。
