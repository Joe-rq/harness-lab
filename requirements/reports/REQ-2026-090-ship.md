# Ship Report: REQ-2026-090

## Scope

- REQ: `REQ-2026-090`
- Release target: Harness Lab 本地候选 npm tarball与仓库交付物
- Operator: Codex（本地验证与交付准备）

## Preconditions

- Review: Approved，见 `requirements/reports/REQ-2026-090-code-review.md`
- QA: Pass，见 `requirements/reports/REQ-2026-090-qa.md`
- Shared runtime / docs updated: policy、两个 Hook、安装 manifest、npm allowlist、README、fixture 已同步
- Scope note: REQ 明确不执行 npm publish、Git commit 或 push

## Commands and Actions

| Step | Result | Evidence |
|------|--------|----------|
| Syntax / tests | Pass | 三个运行脚本语法通过；`npm test` 共 102 项通过 |
| Governance gates | Pass | `docs:verify`、`check:governance`、doctor 均通过 |
| Package build | Pass | `npm pack --dry-run --json --ignore-scripts`：59 files，115,467 B；包含共享策略模块 |
| Adversarial smoke | Pass | 58 项治理 fixture 覆盖 canonical path、多目标、歧义与 exemption |
| Publish / deploy | Not performed | 不在 REQ 授权范围；未修改 registry 或远端仓库 |

## Risks

- Hook 是治理门禁而非安全沙箱；解释器写入、平台不触发 Hook、TOCTOU 与硬链接风险仍需 OS/容器/CI 兜底。
- 当前实机验证平台为 Darwin arm64；Windows/Linux 真实矩阵属于 P1。
- mode/profile 行为文档将在下一项 P0 用户可执行文档 REQ 中按真实现状校正，本 REQ 不改变风险策略。

## Rollback

- 回滚入口：整体还原共享策略模块、两个消费者、安装/发布清单、README 和测试。
- 本轮没有数据库、远端、registry 或用户项目状态变更。
- 回滚后运行 `npm test`、`npm run docs:verify`、`npm run check:governance` 和 pack 检查。

## Conclusion

- Ready for handoff，未发布。共享路径与全目标门禁满足 REQ-090 的发布准备条件。
