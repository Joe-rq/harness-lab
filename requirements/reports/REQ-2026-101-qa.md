# QA Report: REQ-2026-101

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| 复现（修复前，分类器直调） | Fail（预期） | `classifyBashWrites("git commit -F - <<'MSG'\n交付物：in-progress/reports 的 <reqId>-* 模式\nMSG")` → `{writes:true, unresolved:true, targets:[{raw:'-*'}], operations:['redirect']}` |
| 复现（修复前，hook 端到端） | Fail（预期） | 同一条命令喂给 `node scripts/req-check.js` → exit 2，输出 `🚫 REQ ENFORCEMENT: BLOCKED`（误导性提示 "No active REQ found"） |
| 复现（修复前，单元） | Fail（预期） | `FAIL write-target policy ignores heredoc bodies (REQ-2026-101)`：`AssertionError: heredoc 正文不得产生写操作` |
| `node tests/governance.test.mjs` | Pass | `All governance tests passed (72).`（71 既有 + 1 新增；既有 24+ 条写目标断言全部保持） |
| hook 端到端（修复后） | Pass | 同一条 `<reqId>-*` 提交命令 → `exit=0` |
| `npm test` | Pass | 72 governance + 12 status + 11 audit + 25 event-store |
| `npm run docs:verify` | Pass | 619 markdown；diff-aware 18 changed files / 1 triggered rule（改 tests 需同步 README，已在「已知限制」补 heredoc 边界） |
| `npm run check:governance` | Pass | — |
| `npm run harness:doctor` | Pass | 10 通过 / 3 警告 / 0 失败 / 1 跳过 |
| `npm run req:audit` | Pass | `within baseline (125/125 warnings, no delta)` |

### 新增用例覆盖的形态（12 项断言）

| 形态 | 期望 | 结果 |
|---|---|---|
| `git commit -F - <<'MSG'` + 正文含 `<reqId>-*` | `writes:false / unresolved:false / targets:[]` | ✓ |
| `bash <<'EOF'` + 正文含 `echo x > inside.txt` | `writes:false`（正文不扫描） | ✓ |
| `cat <<'EOF'` + 正文含 `rm -rf a.txt > b.txt` | `targets:[]` | ✓ |
| `cat > out.txt <<'EOF'` | `targets:['out.txt']`（头部重定向仍识别） | ✓ |
| `echo x > real.txt <<'EOF'` | `targets:['real.txt']` | ✓ |
| `cat <<'EOF' … EOF` 后再 `echo x > after.txt` | `targets:['after.txt']`（终止行后照常解析） | ✓ |
| `<<"EOF"` / `<<-EOF`（tab 缩进）/ `<<EOF` | 正文均不产生写操作 | ✓ |
| 未闭合 heredoc | `writes:false / unresolved:false`，不抛错 | ✓ |
| `cat <<< "x > not-a-target.txt"` + 下一行重定向 | `targets:['next.txt']`（here-string 不吞行） | ✓ |

## Requirement Verification

- [x] 原始缺陷复现并修复（分类器 + hook 双层证据）。
- [x] 不误伤真写入（头部/同行的 `>` 仍识别；终止行后命令照常解析）。
- [x] 正文里的 `>` / `>>` / `rm -rf` 不产生 target、不置 unresolved。
- [x] 定界符形态稳健（引号、tab 缩进、无引号、未闭合、here-string 排除）。
- [x] 既有 policy 断言全部通过，未降强度。
- [x] `AGENTS.md` 与 README 写明 heredoc 正文不扫描及其取舍（与解释器写同类）。
- [x] 四道门禁全绿，`req:audit` 基线内无 delta。

## Manual Verification

- 阅读 `stripHeredocBodies` 的消费循环：多个定界符按序消费、未闭合吞到末尾、终止行本身不进入待分词文本。
- 确认 `tokenizeShell` 仅在入口做一次剥离，下游 token 处理逻辑一字未改（diff 可验证）。
- 浏览器/UI：N/A。

## Conclusion

- Pass。同族第三个门禁缺陷关闭；本 REQ 的提交信息刻意再次包含 `<reqId>-*`，作为修复后的活体回归证据。
