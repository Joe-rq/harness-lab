# QA Report: REQ-2026-900

## Scope
- REQ: `requirements/completed/REQ-2026-900-example-status-filter.md`
- Design: `docs/plans/REQ-2026-900-design.md`
- Build / branch: example only

## Environment
- OS / runtime: example only
- Required env: none specific for this example
- Test data: seeded list items with `open` and `closed` states

## Commands Run
| Command | Result | Notes |
|---------|--------|-------|
| `npm run test` | Example | 在真实项目中应执行并记录真实输出 |
| `npm run verify` | Example | 在真实项目中应执行并记录真实输出 |

## Manual Verification
| Scenario | Result | Evidence |
|----------|--------|----------|
| 默认打开列表页时显示全部项目 | Pass | 示例说明 |
| 切换为 `open` 后 URL 更新为 `?status=open` | Pass | 示例说明 |
| 切换为 `closed` 后结果正确收窄 | Pass | 示例说明 |
| 搜索词与状态筛选组合使用 | Pass | 示例说明 |
| 输入非法状态值时回退到 `all` | Pass | 示例说明 |

## Issues
1. 无阻塞问题。本示例假设 code review 中提出的白名单校验和组合测试已补齐。

## Conclusion
- Pass
- 备注：这是脱敏示例，展示的是 QA 报告结构和粒度，不是实际执行记录。
