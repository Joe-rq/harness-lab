# QA: REQ-2026-071

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | `session-start.js` 成功追加 `session_started` 事件 | 通过 |
| 2 | `req-cli.mjs` create/start/block/complete 成功追加对应 REQ lifecycle 事件 | 通过 |
| 3 | 事件写入失败不会阻断 session-start / req-cli 主流程 | 通过 |
| 4 | `harness-install.mjs` 的 CLI / hook 分发清单包含 `scripts/event-store.mjs` | 通过 |
| 5 | 测试覆盖新增写入点，并验证事件文件样例 | 通过 |
| 6 | `npm test`、`npm run docs:verify`、`npm run check:governance` 通过 | 通过 |

## 验证证据

| 类型 | 项目 | 结果 | 摘要 |
|------|------|------|------|
| 命令 | `node tests/event-store.test.mjs` | PASS | event-store 基础 API 测试 5 项通过 |
| 命令 | `node tests/governance.test.mjs` | PASS | governance 测试 16 项通过，包含 req lifecycle / session-start 事件断言 |
| 命令 | `npm test` | PASS | 全量测试通过 |
| 命令 | `npm run docs:verify` | PASS | 文档同步检查通过 |
| 命令 | `npm run check:governance` | PASS | governance check 通过 |
| 人工/浏览器 | 事件 payload 样例核对 | PASS | payload 只包含 fileName/title/phase/reason/progressFound 等小型治理事实 |

## 事件样例

```json
{
  "type": "req_started",
  "source": "cli",
  "reqId": "REQ-2026-001",
  "phase": "implementation",
  "payload": {
    "fileName": "REQ-2026-001-automation-test-req.md"
  }
}
```

## 临时实现与债务影响

- 事件写入目前为 best-effort；projection 阶段需要判断是否对关键事件提升为硬失败。
- `worktree` 字段暂为 root 路径；worktree 聚合阶段需要规范化展示标识。

## 结论

REQ-2026-071 QA 通过。Stage 2 下一步进入 S2-CP3：创建 progress projection REQ。
