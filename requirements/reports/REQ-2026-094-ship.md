# Ship Report: REQ-2026-094

## Scope

- 本地候选包和仓库交付；未 publish、未 push、未升级任何外部项目。

## Evidence

- Review Approved；QA Pass。
- 109 tests、capability/docs/governance、profile/ownership doctor 全部通过。
- 真实候选包完成 fresh install、ownership、upgrade dry-run/apply 和 restore。

## Release Conditions

- 正式 npm publish 时必须使用未发布的新 package version，使 ownership sourceVersion 可区分发行版。
- 发布说明需突出 exit 2 表示“冲突已保留”而非程序崩溃，并要求先 dry-run。
- 不得在发布前把 `.harness/backups/`、测试 fixture 或用户 session 状态纳入 package。

## Rollback

- 目标项目：`harness-install --restore <backup-id>`。
- 模板仓库：移除 managed-upgrade publication/CLI/ownership doctor，恢复 installer 为 profile-only；无远端或 registry 状态需回滚。

## Conclusion

- Ready for handoff，未发布。
