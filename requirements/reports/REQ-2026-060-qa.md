# QA: REQ-2026-060

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | `feature` / `bugfix` / `refactor` skills 明确按当前 worktree 检查活跃 REQ，而不是全局检查 | 通过。三者均改为运行 `npm run req:status` 检查当前 worktree，并提示 `--all` 查看全局状态 |
| 2 | 新增 `source-command-worktree-req` skill，覆盖创建 worktree、进入目录、检查状态、创建/启动 REQ、查看全局状态和收尾 | 通过。新 skill 包含 Step 1-5 和注意事项 |
| 3 | `harness-setup` skill 与 `harness-install` 迁移清单包含新 worktree skill | 通过。安装器 skills 模块复制 `.agents/skills/source-command-worktree-req/SKILL.md`，harness-setup command/skill 同步说明 |
| 4 | README 补充 Claude Code 下“一个 worktree 一个 active REQ”的使用说明 | 通过。README worktree 支持段落新增 Claude Code 使用约定 |
| 5 | 自动化测试覆盖安装器会复制新 skill，`npm test`、`npm run docs:verify`、`npm run check:governance` 通过 | 通过 |

## 实际执行命令

```bash
npm test
npm run docs:verify
npm run check:governance
```

## 执行结果

- `npm test`：通过（governance 14/14，req-status-json 12/12）
- `npm run docs:verify`：通过（405 markdown files checked）
- `npm run check:governance`：通过

## 元反思检查

- [x] 目标实现：四项目标均已落地
- [x] 旧功能保护：只改引导文档、安装器复制清单和测试，不改变 `req-cli` 核心行为
- [x] 逻辑正确性：普通 REQ 类型 skill 与专用 worktree skill 的职责边界清晰
- [x] 完整性：迁移分发、README、harness-setup command/skill、测试均已覆盖
- [x] 可维护性：新增 skill 独立承载 worktree 流程，避免在三个 REQ 类型 skill 中重复完整流程

## 对齐检查

- [x] 目标对齐：实现服务于 Claude Code 下并行 REQ 的使用痛点
- [x] 设计对齐：符合“更新现有 REQ 类型 skill + 新增专用 worktree skill”的设计方案
- [x] 验收标准对齐：所有验收标准均有对应实现和验证记录

## 结论

REQ-2026-060 QA 通过，可以完成。
