# QA Report: REQ-2026-102

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| 复现（修复前，缺章节） | Fail（预期） | `findReqTemplateIssues` 对"缺 背景/验收标准"的样本返回 `[]`；英文标题样本返回 `[]`；空章节样本返回 `[]`——三类全部放行 |
| 复现（修复前，引用误报） | Fail（预期） | 本 REQ 起草时正文引用占位符字面量 → PreToolUse `🚫 REQ ENFORCEMENT: BLOCKED`，`- 目标 still contains template placeholder`（三次改写措辞后才放行） |
| 修复后（引用不再误报） | Pass | 本 REQ 恢复原措辞（正文含占位符字面量引用）后：`validateReqFile(...)` → `{"status":"in-progress","issues":[]}`；写操作放行 |
| 修复后（缺章节必须被拒，课程判据） | Pass | 故意样本 `REQ-2099-999`（缺 `## 目标`）真实执行 `npm run req:start -- --id REQ-2099-999` → `🚫 GOVERNANCE BLOCKED` / `missing required section: 目标 (expected heading: ## 目标)`；样本与豁免随后清理 |
| `node tests/governance.test.mjs` | Pass | `All governance tests passed (74).`（72 既有 + 2 新增） |
| `npm test` | Pass | 74 governance + 12 status + 11 audit + 25 event-store |
| `npm run docs:verify` | Pass | 624 markdown；diff-aware 18 changed files / 1 triggered rule（改 tests 需同步 README，已补"必需章节三类判定 + 行首匹配"说明） |
| `npm run check:governance` | Pass | — |
| `npm run harness:doctor` | Pass | 10 通过 / 3 警告 / 0 失败 / 1 跳过 |
| `npm run req:audit` | Pass | `within baseline (125/125 warnings, no delta)` |

### 新增用例覆盖（2 条，25 项断言）

| 形态 | 期望 | 结果 |
|---|---|---|
| 缺两节 | `missing-section:背景` + `missing-section:验收标准` | ✓ |
| 全英文标题（Background/Goals/Acceptance Criteria） | 三条 `missing-section` | ✓ |
| 空章节（标题在、内容空或只有 HTML 注释） | 三条 `empty-section` | ✓ |
| 三节均为模板占位符 | 三条 `template-placeholder` | ✓ |
| 占位符打头 + 括号追加说明 | 仍判 `template-placeholder` | ✓ |
| 占位符被引用（反引号包裹、句中） | 无 issue | ✓ |
| 写实但简短（一句话背景 + 一行目标） | 无 issue | ✓ |
| 标题在文件末尾 | `empty-section`（不是 missing） | ✓ |
| 缺章节的活跃 REQ 下 PreToolUse 写非治理目录 | exit 2 且消息含"验收标准" | ✓ |

### 回归（实施中一度变红、已恢复）

`req-cli lifecycle` / `real git worktrees` / `published tarball` / `req:block` / `req-check accepts slugged active REQ files` 五个流程用例曾因**误删 `parseReqStatus`** 全红——恢复后全部通过。既有 `req validation detects template placeholders and draft status` 曾因占位符归一化漏掉复选框而红，补齐归一化后通过。

## Requirement Verification

- [x] 缺 `## 目标` 的真实 `req:start` 拒绝（课程判据：故意违规样本必须被抓住）。
- [x] 空章节（含只余 HTML 注释）判 `empty-section`。
- [x] 占位符仍被拒；"占位符打头 + 追加说明"仍算未填。
- [x] 引用占位符不再误报（本 REQ 自身即活体证据）。
- [x] 英文标题判"缺少必需章节"并给出期望标题。
- [x] `req-check.js` 不再自带占位符正则表（阻断消息改为渲染共享问题列表）。
- [x] 既有判定不降强度；四道门禁全绿，基线无 delta。

## Manual Verification

- 阅读 `findReqTemplateIssues` 三类分支：`hasHeading` 判定先于 `getSection`，空章节 `continue` 不再叠加占位符问题（避免重复报）。
- 核对 `printBlockMessage` 现渲染共享 issues，且 draft 提示仍在。
- 浏览器/UI：N/A。

## 临时债务对本 REQ 验收的影响

- **不影响**。本 REQ 的判定与渲染改动已全部落在 `req-validation.mjs` / `req-check.js`，并有双向证据。
- 残留债务（`validateDesignDocument` 的设计文档占位符表仍独立）不在本 REQ 验收范围内：本 REQ 的验收标准只覆盖 REQ 必需章节（`## 背景` / `## 目标` / `## 验收标准`），设计文档校验未被改动，也无行为变化。其退出条件见 REQ 的"临时实现与债务"。

## Conclusion

- Pass。D2 承诺兑现，误报消除，判定单点化；双向证据（该拒的拒、该放的放）均为可复现命令。
