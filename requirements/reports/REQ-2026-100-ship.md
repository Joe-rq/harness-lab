# Ship Report: REQ-2026-100

## Scope

- 模板仓库自身：`scripts/scope-guard.mjs` 的两处判定、门禁契约测试、`AGENTS.md` / `README.md` 的规则说明。未 publish、未打 tag。

## Evidence

- Code Review：Approved（同谱系自审）。
- QA：Pass；含两条真实仓库实测（豁免自举放行、交付物 dogfood 写入）。
- 复现证据：新增 2 条用例在旧实现下分别报"全局豁免文件必须可写"与"约定交付物应自动 allow"。

## Release Conditions

- 发布说明需注明 scope-guard 的两项行为变化：**豁免文件写入免检**；**活跃 REQ 的约定交付物自动 allow**（`requirements/in-progress|reports/<reqId>-*`、`context/experience/<reqId>-*`、`docs/plans/<reqId>-*`）。
- 发布说明需注明解析规则：`## 范围` 段内反引号路径一律参与解析（CANNOT 里是 deny、说明句里是 allow）；已有 REQ 若在 CANNOT 段提到过需要写的路径，需改写进 CAN 段。
- 只读边界 REQ 不受交付物 allow 影响，仍只允许 `requirements/reports/**` 且尊重其 deny。
- 不得把该免检扩大到 `.claude/` 下其他文件（如 `progress.txt`），用例已锁死此边界。

## Rollback

- `git revert` 本 REQ 提交即可：改动集中在 scope-guard 的两个判定点、2 条测试与两处文档；无数据迁移。
- 回滚后 GET 到旧行为：豁免文件需人工在会话外创建（自举死锁回归），其余判定不变。

## Conclusion

- Ready for handoff。人闸恢复自举：豁免可创建、范围可修订、交付物不被误拦；范围外写入的拦截强度未降低。
