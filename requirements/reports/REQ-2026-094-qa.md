# QA Report: REQ-2026-094

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| syntax / manifest sync | Pass | managed-upgrade、installer、doctor、tests；capability allowlist 同步 |
| fresh + reinstall ownership | Pass | SHA-256/sourceVersion/profile；重复字节稳定；用户修改不被重新认领 |
| classification table | Pass | unchanged/adopt/update/add/conflict/stale 均覆盖；second plan 幂等 |
| dry-run | Pass | plan 与 apply 同分类；ownership/files/backups 零写入 |
| partial upgrade | Pass | unmodified 更新，modified 保留；exit 2；complete version 不推进 |
| active state preservation | Pass | progress/settings/events/package/用户文件逐字节保持 |
| backup + restore | Pass | apply 前备份；dry-run/explicit restore；payload hash 防篡改 |
| automatic rollback | Pass | 写后故障注入，managed files 与 ownership 恢复；backup 保留 |
| legacy + profile evolution | Pass | 无 record 安全推断；旧 core profile 按新 manifest closure 迁移 |
| adversarial paths | Pass | invalid ownership/backup id、source missing、target symlink escape 均写前失败 |
| doctor ownership | Pass | source skip；target missing/malformed/mixed 可定位，fail/warn 语义正确 |
| `npm test` | Pass | 109：65 governance + 12 status + 11 audit + 21 event-store |
| `npm run docs:verify` | Pass | 577 Markdown（交付物前），55 changed，4 rules |
| `npm run check:governance` | Pass | 125 个历史 warning，门禁通过 |
| packed bin lifecycle | Pass | 71-file candidate；fresh ownership → upgrade dry/apply → restore |

## Requirement Verification

- [x] fresh/reinstall ownership 含 version/profile/module/hash 且不误认领用户修改。
- [x] dry-run/apply 共用 plan，六类状态确定，冲突/陈旧项报告精确。
- [x] backup-first、atomic replace、source/target rehash、自动恢复和显式 restore 均有证据。
- [x] legacy/active REQ/用户设置/业务 package 与事件状态不丢失。
- [x] malformed record、备份篡改、symlink/path 与故障注入均 fail safe。
- [x] Doctor/README/候选包与完整回归对齐。

## Manual Verification

- 逐项对照 upgrade JSON/Markdown report，冲突原因和 restore 命令可执行。
- 审阅 ownership：只有 profile module files，无 package/settings/progress/events/REQ history。
- 审阅 backup：只含将改文件和 profile/ownership/report 元数据；新安装自动 ignore backups。
- 浏览器/UI：N/A。

## Meta-reflection and Alignment

- [x] 目标、旧功能、逻辑、完整性、可维护性检查通过。
- [x] 目标/设计/验收一致；实现保持“分类而非自动合并”的 P1 边界。

## Conclusion

- Pass。
