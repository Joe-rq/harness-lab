# REQ-2026-037 QA 报告

> 2026-04-25

## 验收标准验证

| # | 标准 | 结果 | 证据 |
|---|------|------|------|
| 1 | TEMPLATE.md 包含 id/title/triggers/severity/verification/status | ✅ | `cat context/invariants/TEMPLATE.md` 含全部 6 个字段 |
| 2 | 24 条现有不变量全部补充 status 字段 | ✅ | `invariant-gate.mjs --scan` 汇总: 24 条 (active: 3 / draft: 20 / deprecated: 1 / 无状态: 0) |
| 3 | `invariant-gate.mjs --scan` 能标记缺失结构化字段的条目 | ✅ | 输出 19 条质量问题（title 日期前缀 + message 复读），正确识别 |
| 4 | `invariant-extractor.mjs --inject` 读取 active 不变量，生成注入文本 | ✅ | 输出 3 条 active 不变量，写入 .claude/.invariant-injections/active-invariants.txt |
| 5 | `session-start.sh` 显示不变量统计行 | ✅ | 输出 "🛡️ 不变量: 24 条 (active: 3 / draft: 20 / deprecated: 1)" |
| 6 | 现有 11 个测试仍通过 | ✅ | `npm test` → All governance tests passed (11) |

## 执行的命令

```bash
node scripts/invariant-gate.mjs --scan
node scripts/invariant-extractor.mjs --inject
node scripts/invariant-extractor.mjs --check --file scripts/req-check.sh
bash scripts/session-start.sh
npm test
```

## 回归测试

- `npm test` 全部 11 个测试通过，无回归

## 发现的问题

1. **非阻塞**：--inject 输出未做长度截断（REQ 约束 <2000 字符），当前 3 条约 300 字符，可接受。建议后续 active 数量增长时加截断。

## 结论

QA 通过。6/6 验收标准满足，无回归。
