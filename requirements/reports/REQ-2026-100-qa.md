# QA Report: REQ-2026-100

## 状态

- ✅ Pass

## 验证证据

| Command / scenario | Result | Evidence |
|--------------------|--------|----------|
| 复现（修复前） | Fail（预期） | `node tests/governance.test.mjs` → `FAIL scope-guard allows exemption file bootstrap (REQ-2026-100)`：`AssertionError: 全局豁免文件必须可写，否则无法创建豁免、也无法修订范围`；`FAIL scope-guard allows active REQ deliverables`：`活跃 REQ 的约定交付物应自动 allow: requirements/in-progress/REQ-2026-951-scope-note.md` |
| `node tests/governance.test.mjs` | Pass | `All governance tests passed (71).`（69 既有 + 2 新增） |
| `npm test` | Pass | 71 governance + 12 status + 11 audit + 25 event-store |
| **实测：真实仓库豁免自举** | Pass | 在本仓库（REQ-100 范围**不含** `.claude/`）执行 `touch .claude/.req-exempt` → 放行；`rm .claude/.req-exempt` → 放行。同一条命令在 REQ-2026-098 期间被 `[ScopeGuard]` 拦截，需人工 `!touch` 解扣 |
| **实测：交付物自动 allow（dogfood）** | Pass | `requirements/reports/REQ-2026-100-code-review.md` 在 REQ 的 `## 范围` **未声明**的情况下写入成功；`[ScopeGuard]` 的 allow 列表同时显示 `requirements/in-progress/REQ-2026-100-*.md`、`requirements/reports/REQ-2026-100-*.md`、`context/experience/REQ-2026-100-*.md`、`docs/plans/REQ-2026-100-*.md` 四条自动模式 |
| 不放宽（用例断言） | Pass | 范围外 `scripts/blocked/deliverable-bypass.txt` 仍 block；其他 REQ 的交付物 `requirements/reports/REQ-2026-952-qa.md` 仍 block；`.claude/progress.txt` 仍 block（免检只限豁免文件本身） |
| 只读边界回归 | Pass | `scope-guard blocks write attempts under read-only REQs` 与 `allows legacy REQs without scope declarations` 均通过（未注入交付物 allow） |
| `npm run docs:verify` | Pass | 615 markdown；diff-aware 19 changed files / 1 triggered rule（`governance-automation`：改 tests 需同步 README，README 已补豁免与交付物规则段） |
| `npm run check:governance` | Pass | — |
| `npm run harness:doctor` | Pass | 10 通过 / 3 警告 / 0 失败 / 1 跳过 |
| `npm run req:audit` | Pass | `within baseline (125/125 warnings, no delta)` |

## Requirement Verification

- [x] 范围不含 `.claude/` 时 `touch .claude/.req-exempt` 放行（用例 + 真实仓库实测）。
- [x] worktree-local 豁免路径免检（用例断言 `.claude/worktrees/wt-1/.req-exempt`）。
- [x] 四类约定交付物自动 allow（用例覆盖 REQ 自身 / reports / experience / design；dogfood 实测覆盖 reports）。
- [x] 不放宽：范围外源码、其他 REQ 交付物、`.claude` 下其他文件仍 block；只读边界语义保持（既有用例通过）。
- [x] `AGENTS.md` 写明解析规则（CANNOT → deny、说明句 → allow、"目录禁止 + 单文件例外"写进 CAN）。
- [x] 四道门禁全绿，`req:audit` 基线内无 delta。

## Manual Verification

- 逐条核对允许列表：新增的四条模式均以 `<reqId>-*` 收窄，无目录级放开。
- 确认免检判定发生在"已解析且在仓内"之后——未解析与仓外目标仍 fail closed（读 `scope-guard.mjs` 的 target 循环）。
- 浏览器/UI：N/A。

## Conclusion

- Pass。三处死锁关闭、一处解析规则文档化，行为只增不减；真实仓库与临时 fixture 两条路径均有证据。
