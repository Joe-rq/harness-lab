# Ship Report: REQ-2026-102

## Scope

- 模板仓库自身：REQ 内容校验（必需章节三类判定 + 占位符行首匹配）、PreToolUse 与 `req:start` 判定单点化、公开说明澄清。未 publish、未打 tag。

## Evidence

- Code Review：Approved（同谱系自审）。
- QA：Pass；含修复前（三类样本全放行、引用误报阻断本 REQ）与修复后（引用通过、缺章节被真实 `req:start` 拒绝）双向证据。
- 活体证据：本 REQ 正文保留了占位符字面量引用（修复前会阻断它自己的写入），修复后校验 `issues: []`。

## Release Conditions

- 发布说明需注明判定收紧：**必需章节缺失、内容为空、仅含模板占位符**三种情况都会阻止 `req:start` 与 PreToolUse 写入；标题必须是 `## 背景` / `## 目标` / `## 验收标准`（英文标题会按"缺少必需章节"拒绝并给出期望标题）。
- 发布说明需注明误差修正：占位符判定改为**行首匹配**——正文里引用占位符（反引号包裹、句中提及）不再误报。此前该误报会阻断任何在正文中讨论模板占位符的 REQ。
- 已安装的消费方：`req-check.js` 与 `req-validation.mjs` 需一并更新（前者现在 import 后者的 `validateReqDocument` / `formatReqIssue`）；仅更新其一会导致 hook 报模块错误。
- 历史 REQ 不受影响；`req:audit` 基线仍为 125 无 delta。

## Rollback

- `git revert` 本 REQ 提交：涉及判定分支、一处共享调用替换、`getSection` 正则与两处文档；无数据迁移。
- 回滚后 D2 fail-open 与引用误报同时回归（缺章节 REQ 会再次绿灯，正文引用占位符会再次被拦）。

## Conclusion

- Ready for handoff。2026-07-16 路线报告的 D2（两条路线共同必修）关闭；门禁判定从此单点（三处消费者共享一份实现与一份渲染）。
