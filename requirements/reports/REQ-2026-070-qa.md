# QA: REQ-2026-070

## 验收标准验证

| # | 标准 | 结果 |
|---|------|------|
| 1 | `scripts/event-store.mjs` 导出事件 schema、`appendEvent`、`readEvents`、`validateEvent` | 通过 |
| 2 | append 写入 JSONL 且自动补齐 `id`、`ts`、`sessionId`、`worktree` | 通过 |
| 3 | 坏 schema 事件不会写入文件，并返回清晰错误 | 通过 |
| 4 | 两个独立事件文件可被读取、合并并按 `ts` / `id` 稳定排序 | 通过 |
| 5 | append 性能测试证明单次写入 < 50ms | 通过 |
| 6 | `npm test`、`npm run docs:verify`、`npm run check:governance` 通过 | 通过 |

## 验证证据

| 类型 | 项目 | 结果 | 摘要 |
|------|------|------|------|
| 命令 | `node tests/event-store.test.mjs` | PASS | All event-store tests passed (5) |
| 命令 | `npm test` | PASS | governance、req-status、req-audit、event-store 测试均通过 |
| 命令 | `npm run docs:verify` | PASS | 文档检查通过 |
| 命令 | `npm run check:governance` | PASS | governance audit 通过，仅保留 baseline legacy warnings |
| 人工/浏览器 | 事件样例人工核对 | PASS | schema 只包含治理事实字段和小型 payload，不记录聊天正文或 prompt |

## 事件样例

```json
{
  "id": "evt_test_append",
  "ts": "2026-05-31T00:00:00.000Z",
  "type": "req_started",
  "reqId": "REQ-2026-070",
  "phase": "implementation",
  "source": "cli",
  "sessionId": "session-a",
  "worktree": "main",
  "payload": {
    "title": "Stage 2"
  }
}
```

## 临时实现与债务影响

- 本 REQ 不接入高频写入点；S2-CP2 将单独接入 `session-start.js` 和 `req-cli.mjs` 的 1-2 个事件写入点。
- 未引入新依赖，未修改现有 progress 主链路。

## 结论

REQ-2026-070 QA 通过。Stage 2 下一步进入 S2-CP2：事件账本 MVP 高频写入点接入。
